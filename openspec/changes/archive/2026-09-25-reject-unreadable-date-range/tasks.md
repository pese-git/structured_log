## 1. Разбор границ

- [x] 1.1 `lib/src/http/time_range.dart`: `parseTimeRange` возвращает `({DateTime? from, DateTime? to})`; нечитаемое значение — `ApiError.invalidRequest` с `details.field`/`reason`, по образцу `parsePageRequest`.
- [x] 1.2 Отклонение переноса: поля вынимаются регуляркой, пересобираются `DateTime.utc` и сверяются. UTC — осознанно: это календарная арифметика, а локальная проба сдвигала бы час, пропущенный переходом на летнее время.
- [x] 1.3 `test/http/time_range_test.dart`: отсутствие границ, обычная граница, только дата, смещение, високосный день против невисокосного года, нечитаемые значения, шесть видов переноса, и то, что ошибка называет именно ту границу, которая её вызвала.

## 2. Маршруты

- [x] 2.1 `routes/logs_route.dart` и `routes/audit_log_route.dart` зовут `parseTimeRange` рядом с `parsePageRequest` — то есть после проверки роли и до обращения к хранилищу.
- [x] 2.2 `test/http/routes/logs_route_test.dart`: отказ на нечитаемой и несуществующей границе, `details.field`, и то, что допустимая граница по-прежнему сужает выборку.
- [x] 2.3 `test/http/routes/audit_log_route_test.dart`: то же, рядом с существующим тестом про неизвестный `action` — он сформулировал этот принцип, а `from`/`to` его нарушали.
- [x] 2.4 Мутациями проверено: отбросить разобранные границы по пути к запросу — краснеют оба положительных теста; отключить проверку переноса — краснеют тесты про несуществующую дату.

## 3. Документация

- [x] 3.1 `docs/api/http-api.md`/`.ru.md`: строка `from`/`to` в таблице параметров логов и списки ошибок обоих эндпоинтов.
- [x] 3.2 Архивировать change после слияния.
