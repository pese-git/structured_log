Технологический стек и внутренняя архитектура кода `structured_log_server`/`structured_log_admin_client` (`feature-first`, слоистая/Clean Architecture, `fpdart`, `freezed`, `cherrypick`, `flutter_bloc`, `dio`+`retrofit`, `fluent_ui`) зафиксированы в `design.md`, decisions 32–38, и применяются во всех задачах ниже с самого скаффолдинга (1.4/11.1) — конкретная файловая раскладка внутри фичи уточняется по месту в процессе реализации (decision 32 намеренно не фиксирует её здесь). Все экраны `structured_log_admin_client` (разделы 12–14/20/22) компонуются из atoms/molecules/organisms `structured_log_admin_ui` (раздел 23, decision 39) — реализация экрана оборачивает эти presentation-виджеты в данные/колбэки конкретной фичи, не дублирует их локально.

## 1. Реструктуризация репозитория и скаффолдинг пакетов _(Этап 0)_

- [x] 1.1 `git mv` четырёх существующих пакетов в `emb/`: `structured_log/` → `emb/structured_log/`, `structured_log_flutter/` → `emb/structured_log_flutter/`, `structured_log_material/` (+ `example/`) → `emb/structured_log_material/`, `structured_log_fluent/` (+ `example/`) → `emb/structured_log_fluent/`; создать пустые `backend/`, `frontend/`, `packages/` (последняя без записи в `melos.yaml`, пока пуста)
- [x] 1.2 Обновить пути в корневом [melos.yaml](../../melos.yaml) (`packages:` на новые пути `emb/...`), [.github/workflows/ci.yml](../../.github/workflows/ci.yml) (`working-directory`/матрица `flutter`-job'а, ключи кэша `hashFiles`), [AGENTS.md](../../AGENTS.md) (раздел «Структура» — переписан под категории `emb`/`backend`/`frontend`/`packages`), корневых README
- [x] 1.3 `dart run melos bootstrap`/`melos run analyze`/`melos run test`/`melos run lint` — подтвердить, что перенос не сломал существующие пакеты (по аналогии с задачей 1.4 в `openspec/changes/add-structured-log-flutter/tasks.md` при первом переходе на монорепо)
- [x] 1.4 Создать `backend/structured_log_server/`: `pubspec.yaml` (зависимости `shelf`, `shelf_router`, `drift`, `sqlite3`, `dart_jsonwebtoken`, `bcrypt`, `mailer`, `structured_log`, `fpdart`, `freezed_annotation`, `json_annotation`; dev-зависимости `drift_dev`, `build_runner`, `freezed`, `json_serializable`), `lib/structured_log_server.dart` (barrel), `lib/src/` — по фиче (`auth/`, `users/`, `projects/`, `logs/`, `live_stream/`, `audit/`, ... — decision 32), `shared`/`common`-слой для сквозных таблиц хранения и HTTP-инфраструктуры, `bin/`, `test/`, `example/`; `LICENSE` скопирован; `*.g.dart`/`*.freezed.dart` добавлены в `.gitignore` пакета (`lib/src/` по фиче создаётся по мере того, как в него попадает первый файл — decision 32 намеренно не фиксирует раскладку заранее, а git не отслеживает пустые директории)
- [x] 1.5 Создать `emb/structured_log_http/`: `pubspec.yaml` (единственная зависимость — `structured_log`), `lib/structured_log_http.dart` (barrel), `test/`, `example/`; `LICENSE` скопирован
- [x] 1.6 Добавить оба новых пакета в `packages:` корневого [melos.yaml](../../melos.yaml); включить их в scope скриптов `analyze`/`format`/`format:check` (не требуют явного scope — применяются ко всем пакетам `packages:`); `test:dart` — scope пока не расширен: оба пакета скаффолдятся с пустым `test/` (без файлов), а `dart test` на пустой директории завершается ошибкой (exit 79) — добавление в scope отложено до первых реальных тестов (2.5/9.5), чтобы не заводить тест-заглушку ради самого факта прохождения CI; задокументирован шаг `dart run build_runner build --delete-conflicting-outputs` для `structured_log_server` (`AGENTS.md`, плюс `melos run generate`, scope `structured_log_server`)
- [x] 1.7 `melos bootstrap`/`dart run build_runner build`/`dart analyze` на пустых новых пакетах

## 2. structured_log_server — мультитенантная схема хранения _(Этап 1)_

- [x] 2.1 Определить drift `Table`-классы (`lib/src/storage/database.dart`): `Users` (с `token_version`, `email` — `nullable` в схеме, по умолчанию 0/`null`, обязателен только на уровне валидации `POST /v1/auth/register`, `email_verified_at` — nullable, по умолчанию `null`, `must_change_password` — по умолчанию `false`, `deleted_at` — nullable, по умолчанию `null`, и `is_primary_admin` — по умолчанию `false`), `Groups`, `Teams`, `TeamMembers`, `Projects` (с `is_blocked`, по умолчанию `false`), `ProjectSecretKeys`, `RoleAssignments`, `RefreshTokens`, `PasswordResetTokens` (`id`, `user_id`, `token_hash`, `created_at`, `expires_at`, `used_at`), `EmailVerificationTokens` (`id`, `user_id`, `token_hash`, `created_at`, `expires_at`, `used_at` — структурно идентична `PasswordResetTokens`), `AuditLogEntries` (`id`, `actor_user_id` — nullable, `action`, `target_type`, `target_id` — nullable, `metadata` — JSON-текст, `created_at`), `ProjectUsage`, `LogEntries` (с `project_id`, `size_bytes`) — согласно `specs/log-server-storage/spec.md`; `@DriftDatabase`-аннотированный класс БД, `PRAGMA journal_mode=WAL` (+`PRAGMA foreign_keys=ON` — не входит буквально в задачу, но нужен, чтобы FK-инварианты раздела 6 хранения реально проверялись SQLite, а не только объявлялись); `DateTime`-колонки хранятся как TEXT (`build.yaml`, `store_date_time_values_as_text`), не lossy unix-секундами по умолчанию — исходный `timestamp` требует точности до микросекунд (round-trip fidelity, требование `specs/log-server-storage`); сгенерирован `database.g.dart`
- [x] 2.2 Индексы: `log_entries` — `project_id`/`timestamp`/`level`/`category`/`session_id`/`request_id` и составной `(project_id, level, timestamp)`; уникальный индекс на `users.username` (глобальный, без исключения по `deleted_at` — soft-delete не освобождает имя); уникальный частичный индекс на `users.email` (`WHERE email IS NOT NULL`, тоже без исключения по `deleted_at`); уникальный частичный индекс на `users.is_primary_admin` (`WHERE is_primary_admin = true` — гарантия на уровне схемы, что второй `INSERT`/`UPDATE` с этим флагом не пройдёт, даже если проверка в коде 8.3 будет обойдена); индекс на `refresh_tokens.user_id`; индекс на `password_reset_tokens.user_id`; индекс на `email_verification_tokens.user_id`; индексы на `audit_log_entries.actor_user_id`, `audit_log_entries.action`, `audit_log_entries.(target_type, target_id)` и `audit_log_entries.created_at` (композитные/частичные индексы drift'а `@TableIndex` не выражают напрямую — созданы raw SQL в `MigrationStrategy.onCreate`, тем же приёмом, что уже принят для `LIKE`/`json_extract`, decision 3 `design.md`)
- [x] 2.3 Миграция схемы при старте (`MigrationStrategy`/`onUpgrade`, идемпотентная) — на `schemaVersion = 1` `onUpgrade` пока не от чего мигрировать; идемпотентность повторного старта — встроенное поведение drift (`onCreate` выполняется только когда `user_version` файла базы ещё не установлен), подтверждено тестом на сохранность данных после рестарта (2.5)
- [x] 2.4 Реализовать `LogStore`/`DriftLogStore` (`insertBatch` с привязкой к `project_id`, `query` с фильтрами из `specs/log-server-api`) и построение фильтра (`lib/src/storage/query.dart`): типизированные поля через query-builder drift, `LIKE`/`json_extract` через `customSelect`, keyset-пагинация по `id` (реализовано как один собранный `customSelect`, а не смесь builder+customSelect — динамическое число условий, включая произвольное число `context.<key>`-фильтров, проще собрать одним SQL-текстом с позиционными `?`, чем комбинировать типизированный builder с фрагментами; `LogQuery.projectIds` — уже разрешённая, авторизованная область видимости, резолвинг `project_id`/`group_id` и проверка `is_blocked`/RBAC — забота вызывающего слоя, раздел 6, не storage)
- [x] 2.5 Юнит-тесты storage-слоя на сценарии из `specs/log-server-storage/spec.md`: привязка записи к проекту, использование составного индекса, round-trip произвольного context-поля, независимость received_at от timestamp, согласованность scope_id в role_assignments, сохранность данных после рестарта, отсутствие блокировки чтения при конкурентной записи (WAL) — `test/storage/database_test.dart` (19 тестов) и `test/storage/log_store_test.dart` (14 тестов на `LogStore`: привязка к проекту, каждый фильтр `LogQuery` по отдельности, экранирование `LIKE`-метасимволов в `q`, строковое/числовое совпадение `context.<key>`, keyset-пагинация без пропусков/дублей, откат всего батча при ошибке одной записи)

## 3. structured_log_server — аутентификация (log-server-auth) _(Этап 1 — частично, см. design.md «Delivery Phases»)_

- [x] 3.1 Определить публичный интерфейс `IdentityProvider`/`VerifiedIdentity`/`EffectiveRole` (`lib/src/auth/identity_provider.dart`, экспортирован из barrel-файла пакета) — контракт `verifyAccessToken(String bearerToken) -> Future<VerifiedIdentity?>`, `VerifiedIdentity.roles` опционален
- [x] 3.2 Хэширование паролей (`bcrypt`), секретных ключей проектов и refresh-токенов (SHA-256 случайного токена, генерируемого `Random.secure()`) — `lib/src/auth/hashing.dart`
- [ ] 3.3 _(не в Этапе 1 — см. design.md «Delivery Phases»)_ `POST /v1/auth/register`: создание пользователя по `username`/паролю/обязательному `email` (опционально `display_name`) без `RoleAssignment`, доступен только при `ServerConfig.registrationEnabled == true` (403 иначе), 400 при отсутствии `email`, 409 при занятом `username` или `email`; при успехе — аудит-запись `user.created` (актор — сам созданный пользователь, раздел 19) и выпуск токена подтверждения email через общую функцию 24.2 (раздел 24, `log-server-email-verification`); `must_change_password` не устанавливается (только `POST /v1/users`/`PATCH /v1/users/:id`, раздел 26)
- [x] 3.4 Резолвинг claims `roles`/`tv` (`lib/src/auth/claims.dart`): по `user_id` собрать плоский снапшот эффективных прав (прямые `role_assignments` + через `team_members`) и прочитать текущий `token_version` — общая функция, используемая и `grant_type=password`, и `grant_type=refresh_token` (каждый раз заново, не переносится из старого токена)
- [x] 3.5 Бизнес-логика диспетчеризации по `grant_type` реализована (`lib/src/auth/token_service.dart`, `TokenService.passwordGrant`/`refreshTokenGrant`), сам HTTP-эндпоинт `POST /v1/auth/token` (form-encoded, RFC 6749 §5.1/§5.2 JSON) собирается в разделе 6.4 вместе со всеми остальными маршрутами — отдельной подзадачи на неё в разделе 3 нет:
  - `grant_type=password` — проверка `username`/`password`; проверка `email IS NULL OR email_verified_at IS NOT NULL` **опущена в Этапе 1** (email/`log-server-email-verification` не входят в Этап 1, design.md «Delivery Phases» — в Этапе 1 email ни у кого нет) — выдача access-JWT (`dart_jsonwebtoken`, HS256, подписывающий секрет/issuer передаются в `TokenService` конструктором — источник значений, `ServerConfig`, появится в разделе 8) с claims `iss`/`sub`/`iat`/`exp`/`jti`/`preferred_username`/`tv`/`roles` (из 3.4) + refresh-токена (хранится хэшем в `refresh_tokens`)
  - `grant_type=refresh_token` — проверка хэша/срока/`revoked_at` refresh-токена **и `is_active` пользователя**, ротация (отзыв предъявленного + выдача новой пары с заново резолвленными `roles`/`tv` из 3.4), отзыв всех refresh-токенов пользователя при повторном использовании уже отозванного — реализовано целиком
  - Рендеринг тела ответа в форму RFC 6749 §5.1/§5.2 — часть HTTP-слоя, раздел 6.4; `TokenService` возвращает типизированный `Either<TokenError, TokenPair>`, не JSON
- [x] 3.6 Бизнес-логика `TokenService.revoke` реализована: немедленный отзыв предъявленного refresh-токена, идемпотентно (валидный/невалидный/несуществующий токен — одинаковое поведение, не бросает); сам HTTP-эндпоинт `DELETE /v1/auth/token` (включая 400 `invalid_request` при отсутствии поля `refresh_token` — забота парсинга form-encoded тела) — раздел 6.4
- [x] 3.7 Реализовать `LocalIdentityProvider implements IdentityProvider` (`lib/src/auth/local_identity_provider.dart`), оборачивающий 3.4–3.6: `verifyAccessToken` проверяет подпись/срок действия JWT, делает point-lookup `token_version` пользователя по `sub` и сравнивает с claim `tv` (несовпадение → `null`, как любой невалидный токен — эта проверка не часть публичного интерфейса), при совпадении возвращает `VerifiedIdentity` с непустым `roles` из claims
- [x] 3.8 Access-JWT-auth middleware для management-эндпоинтов и `GET /v1/logs` (`lib/src/http/auth_middleware.dart`): вызывает `IdentityProvider.verifyAccessToken` (текущая конфигурация — `LocalIdentityProvider` из 3.7), 401 при `null`; если `VerifiedIdentity.roles` не `null` — авторизация читает роли прямо оттуда, без обращения к `role_assignments`/`team_members`
- [x] 3.9 Auth middleware приёма логов (`lib/src/http/project_key_middleware.dart`): резолвинг секретного ключа проекта из `Authorization: Bearer`, 401 при отсутствии совпадения или `revoked_at != null` (отдельный путь, не через `IdentityProvider`)
- [x] 3.10 _(Этап 3 — см. design.md «Delivery Phases»)_ Общая функция удаления пользователя (`lib/src/auth/delete_user.dart`), используемая и 3.10a, и 3.10b: сначала проверка `is_primary_admin` цели (point-lookup по `id`) — если `true`, немедленно 403 `cannot_delete_primary_admin`, без дальнейших проверок и изменений; иначе — проверка единственного владельца группы (`SELECT` по `role_assignments`/`team_members` — есть ли группа `G`, где у цели `role: owner`, и нет другого `owner` на ту же `G`; если да — 409 `sole_group_owner` со списком `G`, без изменений), затем атомарно — `deleted_at = now()`, `is_active = false`, отзыв всех `refresh_tokens` пользователя (как 4.2/4.5 для блокировки), инкремент `token_version`, удаление прямых `role_assignments` (`subject_type: user`, `subject_id` = цель) и записей `team_members` цели
- [x] 3.10a _(Этап 3)_ `DELETE /v1/users/me` (JSON `{"password": "..."}"`, доступен любой аутентифицированной роли только над собой): проверка текущего пароля (401/400 при неверном), затем 3.10; при успехе — аудит-запись `user.deleted` (актор и цель — сам пользователь, раздел 19); при 409/403 из 3.10 (включая `cannot_delete_primary_admin`) — аудит-запись не создаётся
- [x] 3.10b _(Этап 3)_ `DELETE /v1/users/:id` (только `admin` — авторизация 4.7, без тела запроса/пароля цели): 400 при `:id == вызывающий` (самоудаление — только через 3.10a), иначе 3.10; при успехе — аудит-запись `user.deleted` (актор — `admin`, цель — удалённый пользователь, раздел 19); при 409/403 из 3.10 (включая `cannot_delete_primary_admin`) — аудит-запись не создаётся
- [x] 3.11 Юнит-тесты на сценарии из `specs/log-server-auth/spec.md`, ограниченные тем, что реализовано в Этапе 1 (без регистрации/удаления аккаунта — 3.3/3.10): `grant_type=password`/`refresh_token` (успех/ошибка), заблокированный (`is_active = false`) пользователь не может получить новый access-токен через `refresh_token` даже с действительным refresh-токеном, ротация refresh-токена, отзыв всей цепочки при реюзе отозванного refresh-токена, `TokenService.revoke` (включая одинаковое поведение на валидный/невалидный/несуществующий токен), отсутствие plaintext-пароля/секрета в БД (`hashing_test.dart`), состав `roles` (прямые + через команду), 401 при несовпадении `token_version`, актуальные `roles`/`tv` после `refresh`, `LocalIdentityProvider.verifyAccessToken` возвращает `VerifiedIdentity` с непустым `roles` (контракт `IdentityProvider`), обе HTTP-middleware (`auth_middleware_test.dart`/`project_key_middleware_test.dart`) — `test/auth/`, `test/rbac/`, `test/http/` (58 новых тестов сверх раздела 2)

## 4. structured_log_server — RBAC (log-server-rbac) _(Этап 1 — частично, см. design.md «Delivery Phases»)_

- [x] 4.1 Резолвинг эффективных прав пользователя из хранилища (`lib/src/rbac/authorizer.dart`, переиспользуется `claims.dart` из 3.4): прямые `role_assignments` пользователя + через членство в командах, с учётом иерархии областей (global ⊇ group ⊇ project) — единственный источник истины (см. `specs/log-server-rbac`, requirement «Резолвинг... не зависит от источника аутентификации»); вызывается и при выдаче токена (3.4), и как fallback-путь из 4.4, когда `VerifiedIdentity.roles == null`
- [x] 4.2 `lib/src/rbac/token_version.dart`: пока только одиночный инкремент (`incrementTokenVersion`) — единственный вызывающий в Этапе 1 (смена пароля, раздел 26) мутирует ровно одного пользователя, не команду. Каскадный bulk-`UPDATE` для всех участников команды при изменении team-scoped `RoleAssignment`/членства не реализован — вызывающего (раздел 4.3 — выдача ролей — не в Этапе 1) для него нет; добавляется вместе с 4.3 в последующем этапе, не раньше
- [x] 4.3 _(Этап 4 — полная версия, см. design.md «Delivery Phases»; 4.3a — урезанный срез Этапа 3)_ Правила выдачи/отзыва ролей (`POST`/`DELETE /v1/role-assignments`): admin — без ограничений; owner группы `G` — только role ∈ {owner, user} на scope ∈ {group:G, project ∈ G}; user — запрещено полностью; создание/удаление вызывает 4.2
- [x] 4.3a _(Этап 3 — урезанная версия 4.3)_ Правила выдачи/отзыва ролей в объёме этого этапа: только `admin` (без права owner-а группы выдавать роли в её рамках — эта часть 4.3 остаётся будущему этапу вместе с командами/ролями) и только `subject_type: user` (не `team` — команд, раздел 5.3, ещё нет); создание/удаление вызывает одиночный (не каскадный) инкремент `token_version` (4.2)
- [x] 4.4 Authorization middleware для management-эндпоинтов и `GET /v1/logs` — **реализовано не middleware, а проверками внутри хендлеров** (`resolveRoles` + `canRead`/`canWrite` из `lib/src/rbac/access_check.dart`, вызываются первой строкой каждого хендлера), как и предписывает decision 4.4 `design.md`; `VerifiedIdentity.roles` из токена используются напрямую, при `roles == null` роли резолвятся из хранилища (`Authorizer`). Тесты: `test/rbac/access_check_test.dart` (полная сетка ролей и областей), плюс по маршрутам. Числилась незакрытой по недосмотру — обнаружено сверкой 16.3
- [x] 4.5 _(Этап 3)_ Authorization-правило для блокировки (`admin` only, decision 25 `design.md`): `POST /v1/users/:id/block`/`unblock` и `POST /v1/projects/:id/block`/`unblock` доступны только `RoleAssignment(role: admin, scope: global)` — 403 даже для `owner` группы блокируемого проекта/пользователя; `block` пользователя вызывает 4.2 (инкремент `token_version`) и дополнительно отзывает все его `refresh_tokens` (`revoked_at`); `unblock` дополнительно проверяет `deleted_at IS NULL` — 400, если пользователь удалён — самостоятельно или администратором (decision 27, не переактивировать удалённых через block/unblock)
- [x] 4.7 _(Этап 3)_ Authorization-правило для удаления чужой учётной записи (`admin` only, decision 27 `design.md`): `DELETE /v1/users/:id` (3.10b) доступен только `RoleAssignment(role: admin, scope: global)` — 403 даже для `owner` группы удаляемого пользователя
- [x] 4.6 Юнит-тесты на сценарии из `specs/log-server-rbac/spec.md`, ограниченные тем, что реализовано в Этапе 1: наследование прав по иерархии областей, появление прав нового участника команды (`test/rbac/authorizer_test.dart`), прямые и team-inherited роли одновременно, роль другой команды не наследуется. Каскадный инкремент `token_version`/выдача-отзыв ролей/блокировка/удаление — не в Этапе 1 (4.3/4.5/4.7), тесты на них отложены вместе с ними

## 5. structured_log_server — management API _(Этап 1 — частично, см. design.md «Delivery Phases»)_

- [x] 5.1 _(Этап 3 — email исключён из объёма, см. design.md «Delivery Phases»)_ `POST /v1/users` без поля `email` (admin only; создание — аудит `user.created`, раздел 19; `must_change_password = true` всегда — раздел 26), `GET /v1/users` (admin only); `PATCH /v1/users/:id` (admin only — раздел 26, только `display_name`/`password`, без `email`); `POST /v1/users/:id/block` (`is_active = false`, инкремент `token_version` — 4.2, отзыв всех `refresh_tokens` пользователя, аудит `user.blocked`) и `POST /v1/users/:id/unblock` (`is_active = true`, аудит `user.unblocked`) — admin only (4.5); `DELETE /v1/users/:id` (3.10b) — admin only (4.7)
- [x] 5.2 `POST /v1/groups`, `GET /v1/groups` (создание — admin only, список — по видимым вызывающему группам) — `lib/src/http/routes/groups_route.dart`. Аудит `group.created` не пишется — раздел 19 (аудит) не в Этапе 1, см. ниже
- [ ] 5.3 _(Этап 4)_ `POST /v1/groups/:groupId/teams` (аудит `team.created`), `POST /v1/teams/:teamId/members` (аудит `team.member_added`), `DELETE /v1/teams/:teamId/members/:userId` (аудит `team.member_removed`) (owner группы/admin; оба последних вызывают каскадный инкремент `token_version` участников, 4.2)
- [x] 5.4 `POST /v1/groups/:groupId/projects` (создание с обязательным `retention_days`, опциональными `max_entries`/`max_bytes` — `specs/log-server-quotas`, атомарно создаёт и строку `ProjectUsage`), `PATCH /v1/projects/:id` (частичное изменение квоты), `GET /v1/projects/:id` (owner группы/user с доступом/admin; ответ включает `entry_count`/`total_bytes` из `ProjectUsage` рядом с квотой — decision 22 `design.md`; включает `is_blocked`) — `lib/src/http/routes/projects_route.dart`. `POST /v1/projects/:id/block`/`unblock` — **не в Этапе 1** (см. 5.4a): авторизация для них (4.5) не входила в Этап 1, а блокировка/разблокировка без него не имела смысла. Аудит (`project.created`/`project.quota_updated`) не пишется — раздел 19 не в Этапе 1
- [x] 5.4a _(Этап 3)_ `POST /v1/projects/:id/block` (`is_blocked = true`) и `POST /v1/projects/:id/unblock` (`is_blocked = false`) — admin only, та же authorization-проверка, что и для блокировки пользователя (4.5, decision 25 `design.md`); аудит `project.blocked`/`project.unblocked` (раздел 19, уже реализован с Этапа 2)
- [x] 5.8 _(добавлено 15.09.2026 — обнаружено при старте раздела 14)_ `GET /v1/projects` — плоский список проектов, доступных вызывающему, с необязательным `group_id`. Перечислить проекты было **нечем**: существовали только `GET /v1/groups` и `GET /v1/projects/:id`, поэтому селектор области (14.1) и экран проектов (13.3) не на чем было построить. Плоский, а не `GET /v1/groups/:id/projects`: роль, выданная на проект, не покрывает его группу (`_covers` в `lib/src/rbac/access_check.dart`), так что пользователь с доступом к одному проекту не видит ни одной группы — вложенный эндпоинт оставил бы его вовсе без входа. Счётчики использования в список не попадают (остаются у `GET /v1/projects/:id`), заблокированные проекты попадают с `is_blocked`. Требование и четыре сценария добавлены в `specs/log-server-api`; маршрут внесён в `test/http/route_auth_matrix_test.dart` — страж поймал его отсутствие сразу, как и задумано
- [x] 5.5 `POST /v1/projects/:id/secret-keys` (создание/ротация — ключ в открытом виде только в ответе на создание), `GET /v1/projects/:id/secret-keys` (только метаданные), `DELETE /v1/projects/:id/secret-keys/:keyId` (отзыв) — `lib/src/http/routes/secret_keys_route.dart`. `label` сделан nullable в схеме хранения (раздел 2) вслед за `docs/api/models.md` — расхождение с исходной формулировкой `specs/log-server-storage/spec.md`, где `label` не был явно помечен nullable, но и не был явно обязателен; `docs/api/http-api.md` также документирует его как опциональное поле запроса. Доступ на чтение (`GET`) дан ролям `admin`/`owner`/`user` с доступом к проекту (по буквальной формулировке `specs/log-server-rbac`: «user только чтение (GET), не изменение») — изначально это было шире, чем `docs/api/http-api.md`, ограничивавший `GET` только `owner`/`admin`; расхождение проверено (design.md/architecture-доки не содержат обоснования для более узкого правила именно на чтение — похоже на недосмотр при первом написании `http-api.md`, а не осознанное сужение) и устранено правкой `docs/api/http-api.md`/`docs/api/http-api.ru.md` под формулировку спеки (тот же паттерн, что уже используется для `GET /v1/projects/:id`: `owner`/`user` с доступом, или `admin`). Аудит (`secret_key.created`/`secret_key.revoked`) не пишется — раздел 19 не в Этапе 1
- [ ] 5.6 _(Этап 4 — полная версия, см. design.md «Delivery Phases»; 5.6a — урезанный срез Этапа 3)_ `POST /v1/role-assignments` (аудит `role_assignment.created`), `DELETE /v1/role-assignments/:id` (аудит `role_assignment.revoked`) — вызывает authorization middleware (4.4) и правила выдачи (4.3), сама операция вызывает инкремент `token_version` (4.2: одиночный для subject_type=user, каскадный для subject_type=team)
- [x] 5.6a _(Этап 3)_ `POST /v1/role-assignments` (аудит `role_assignment.created`), `DELETE /v1/role-assignments/:id` (аудит `role_assignment.revoked`) — только `subject_type: user`, доступно только `admin` (4.3a); без этой задачи `POST /v1/users` (5.1) создаёт учётную запись без единого `RoleAssignment`, а `specs/log-server-rbac` («user без явного гранта на ресурс не имеет доступа») делает её нерабочей до выдачи роли — способа выдать которую иначе в Этапе 3 не было бы
- [x] 5.7 Унифицировать формат ошибок (`lib/src/errors.dart`): `ApiError` (код/сообщение/`details`/HTTP-статус) → общий JSON-конверт `log-server-api`, плюс `errorHandlingMiddleware`, перехватывающий брошенный `ApiError` и рендерящий его — маршруты `throw`, не строят ответ на каждом пути вручную. Реализованы конструкторы для кодов, реально используемых в Этапе 1 (`invalid_request`/`unauthorized`/`forbidden`/`not_found`); остальные коды каталога (`docs/api/errors.md`) добавляются вместе с эндпоинтами, которые их производят

## 6. structured_log_server — приём и запрос логов (log-server-api) _(Этап 1)_

- [x] 6.1 `POST /v1/logs`: auth секретным ключом проекта, проверка `Project.is_blocked` (403 `project_blocked`, приоритетнее валидации/квоты — раздел 7), валидация записей батча, частичный приём с отчётом об отклонённых (`validation_error`/`quota_exceeded` из раздела 7), 413 при превышении размера тела — `lib/src/http/routes/logs_route.dart` (`ingestLogs`), валидация/квота в `lib/src/ingest/ingest.dart`. Тело батча-ответа всегда `202` (включая `quota_exceeded`-отказы внутри `rejected[]`) — вслед за `docs/api/errors.md`, уточняющим формулировку `specs/log-server-quotas` («HTTP-статус `507`») до «507 не появляется как top-level статус, только код внутри `rejected[]`»; размер тела — `maxBodyBytes` параметр (заглушка на месте будущего `ServerConfig`-поля, раздел 8)
- [x] 6.2 `GET /v1/logs`: JWT-auth, обязательный `project_id` или `group_id`, RBAC-проверка (`lib/src/rbac/access_check.dart`, см. раздел 4.4); при `project_id` заблокированного проекта — 403 `project_blocked` (перекрывает обычную авторизацию); при `group_id` — заблокированные проекты молча исключаются из агрегированного результата (пустой список проектов после исключения — пустая страница результатов, не ошибка); остальные фильтры и keyset-пагинация из `specs/log-server-api/spec.md`; без курсора — по убыванию `id`, курсор продвигает выборку к более ранним записям — `lib/src/http/routes/logs_route.dart` (`queryLogs`)
- [x] 6.3 `GET /healthz` без аутентификации — `lib/src/http/routes/logs_route.dart` (`healthCheck`)
- [x] 6.4 Собрать сервер (`lib/src/http/server.dart`, `buildHandler`): `Pipeline` + `errorHandlingMiddleware` (раздел 5.7) + `shelf_router`; JWT-auth/secret-key-auth применяются не общим middleware на весь роутер (одни и те же пути — `/v1/logs` — обслуживают оба метода с разной схемой аутентификации), а поточечно на каждый маршрут через `Middleware`-обёртку хендлера (`jwtAuth(handler)`/`projectKeyAuth(handler)`); авторизация (RBAC) — внутри каждого хендлера, не отдельным слоем (см. 4.4). `shelf_io.serve` — часть CLI entrypoint, раздел 8, не этой задачи (`buildHandler` тестируется, вызывая `Handler` напрямую, без реального сокета)
- [x] 6.5 Юнит-тесты на сценарии из `specs/log-server-api/spec.md`, ограниченные тем, что реализовано в Этапе 1: приём/отказ по ключу, приём в заблокированный проект (403 `project_blocked`), частичный приём, лимит размера, доступ по project_id/group_id (включая 403/404), запрос к заблокированному проекту напрямую (403) и в составе `group_id` (молчаливое исключение, включая случай «после исключения проектов не осталось»), фильтры (включая `context.<key>`), структура ошибок — `test/logs/ingest_test.dart`, `test/http/routes/logs_route_test.dart`; `test/http/server_test.dart` — сквозной сценарий через собранный `Handler` целиком (без реального HTTP-сокета): вход → создание группы/проекта/ключа → приём → запрос
- [x] 6.6 **Прикладные поля с зарезервированными именами больше не теряются молча.** `GET /v1/logs` собирает ответ как `{...context, 'id', 'project_id', 'received_at'}` поверх контекста, где лежит присланная запись целиком, поэтому собственное поле приложения с таким именем сохранялось в базе, но в ответе затиралось серверным значением — и ничто об этом не сообщало. Формат оставлен как есть (решение мейнтейнера), поэтому починка на приёме: `reservedEntryFieldNames` в `lib/src/ingest/ingest.dart`, запись с таким полем отклоняется как невалидная **поштучно**, с указанием имени, а остальные записи батча принимаются. Спека дополнена сценарием в `specs/log-server-api`- [x] 6.7 Тест-страж отсутствия CORS (`test/http/cors_test.dart`): ответ на запрос с заголовком `Origin` не содержит `Access-Control-Allow-Origin` ни на публичном, ни на аутентифицированном эндпоинте. Само отсутствие CORS не реализуется — реализуется его *необратимость по недосмотру*: требование в `specs/log-server-api` определяет способ развёртывания (клиент за тем же прокси, что API), а middleware, добавленный мимоходом, поменял бы его молча

## 7. structured_log_server — квоты (log-server-quotas) _(Этап 1)_

- [x] 7.1 `ProjectUsage`: инициализация при создании проекта (раздел 5.4, атомарно в той же транзакции), атомарное обновление в транзакции вставки батча (раздел 6.1, `ProjectUsageCompanion.custom` с SQL-инкрементом, без гонки read-modify-write) — обновление при периодической очистке ещё не применимо (7.3 не реализован)
- [x] 7.2 Проверка `max_entries`/`max_bytes` на приёме, отказ отдельным записям батча с `quota_exceeded` (интегрировано с разделом 6.1) — `lib/src/ingest/ingest.dart`, `processIngestBatch`; лимиты применяются кумулятивно внутри одного батча (не только к снапшоту `project_usage` до батча)
- [x] 7.3 Периодический purge job по `retention_days` (конфигурируемый интервал), уменьшающий `ProjectUsage` — `lib/src/retention/purge_job.dart`: `purgeExpiredEntries` (горизонт считается для каждого проекта отдельно — общий отсечкой был бы неверен при разных `retention_days`) и `PurgeScheduler` (таймер живёт в `bin/server.dart`, останавливается в shutdown до закрытия БД). Удаление идёт ограниченными порциями, каждая порция и уменьшение счётчиков — в одной транзакции: один `DELETE` на миллионы строк застопорил бы единственный isolate (decision 4), а падение между удалением и декрементом навсегда завысило бы `project_usage`. Счётчики зажаты снизу нулём. Пустой проход логируется на уровне `debug` (`retention.purge_completed`), непустой — `info` (`retention.purged`): иначе «прошёл ли проход» ненаблюдаемо снаружи, а на уровне `info` здоровый сервер печатал бы это вечно. Раздел 31 (очистка аудита) добавит свои шаги в этот же job, новый таймер не заводя
- [x] 7.4 Юнит-тесты на сценарии из `specs/log-server-quotas/spec.md`, ограниченные тем, что реализовано: обязательность `retention_days` (раздел 5.4), проект без лимита объёма, отказ при превышении `max_entries`/`max_bytes` (включая учёт внутри одного батча) — `test/logs/ingest_test.dart`; `PATCH` корректно сохраняет новое значение квоты — `test/http/routes/projects_route_test.dart` (сквозной сценарий «отказ по `quota_exceeded` → `PATCH` поднимает лимит → повторный батч проходит» отдельно не тестировался, косвенно покрыт: `processIngestBatch` уже протестирован с разными значениями `maxEntries` при том же `currentEntryCount`); удаление по истечении retention — не в Этапе 1 (7.3)

## 8. structured_log_server — CLI entrypoint и конфигурация (log-server-config) _(Этап 1)_

- [x] 8.1 `ServerConfig` (`lib/src/config/server_config.dart`) — реализовано полное подмножество Этапа 1: `db-path`/`http-host`/`http-port`/`jwt-signing-secret`/`jwt-issuer`/`max-ingest-body-bytes`/`retention-purge-interval-seconds`/`bootstrap-admin-enabled`/`bootstrap-admin-username`/`bootstrap-admin-password`/`log-level`/`log-file`/`log-format`/`log-max-file-bytes`/`log-max-files`/`rate-limit-enabled`/`rate-limit-bucket-capacity`/`rate-limit-refill-per-minute`/`rate-limit-max-keys`/`trusted-proxy-hops`/`sse-heartbeat-interval-seconds`. Поля для отложенных фич (`registrationEnabled`, SMTP, `passwordResetBaseUrl`+срок, `emailVerificationBaseUrl`+срок, `auditRetentionDays`/`authEventRetentionDays`+размер порции) — не в Этапе 1, добавляются вместе со своими разделами
- [x] 8.2 `bin/server.dart`: обычный запуск сервера (`serve`), graceful shutdown по `SIGINT`/`SIGTERM` (`SIGTERM` — не на Windows, `ProcessSignal.sigterm.watch()` там не поддерживается)
- [x] 8.3 Команда `create-admin` (`lib/src/auth/create_admin.dart`) — сохраняется наряду с автосозданием (раздел 34); переиспользует те же `bootstrap-admin-username`/`bootstrap-admin-password`, не отдельные флаги (секрет — по-прежнему только env/`*_FILE`, требование 8.7 «для create-admin обязателен только путь к БД» соблюдено буквально — пароль не required на уровне резолвера; при незаданном пароле `create-admin` генерирует его тем же путём, что автосоздание, и ставит `must_change_password = true`; при заданном операторам — `must_change_password` не ставится, decision 49). Проверка «уже существует ли admin» учитывает только активных, неудалённых (`is_active = true`, `deleted_at IS NULL`) администраторов (decision 12/27); `is_primary_admin = true` устанавливается только если ни у одного пользователя в таблице (включая удалённых) этот флаг ещё не установлен (decision 28, плюс уникальный частичный индекс из 2.2 как вторая линия защиты); при последующих вызовах — `is_primary_admin = false`

- [x] 8.4 Резолвер конфигурации (`lib/src/config/config_resolver.dart`) на `package:args`: единая декларация параметров (`lib/src/config/param_spec.dart` — имя, тип, значение по умолчанию, описание, обязательность по команде, признак секрета), из которой порождаются и парсер CLI, и имена переменных окружения (`--kebab-case` → `STRUCTURED_LOG_KEBAB_CASE`), и вывод `--help`; приоритет CLI → env → default (decision 47 `design.md`)
- [x] 8.5 Резолюция секретов (`lib/src/config/secret_source.dart`): env-переменная и её `*_FILE`-вариант (чтение файла с обрезкой завершающего перевода строки); CLI-флага для секретов не существует; одновременное задание обеих форм — ошибка конфигурации; отсутствие JWT-секрета для команды `serve` — ошибка (JWT-секрет required только для `serve`, не для `create-admin`)
- [x] 8.6 Строгий разбор значений: целые (единица в имени параметра — `*-seconds`/`*-bytes`), булевы (`true/false/1/0/yes/no` без учёта регистра в env; negatable-флаги в CLI), nullable-параметры (пустая строка в env = не задано; `log-file` без значения = консоль); любое нераспознанное значение — ошибка, а не молчаливый дефолт; строковые параметры с закрытым набором значений (`log-level`/`log-format`) — вне набора — тоже ошибка
- [x] 8.7 Валидация до открытия порта и до создания файла БД (сама валидация — до любого обращения к `StructuredLogDatabase.open`): накопление **всех** ошибок и вывод списком, код выхода `78` (`exitCodeConfigError`, `EX_CONFIG`); набор обязательных параметров вычисляется по подкоманде (`ParamSpec.requiredForCommands`) — для `create-admin` обязателен только `db-path`
- [x] 8.8 Неизвестный CLI-аргумент — ошибка запуска (ловится как `FormatException` от `package:args`, превращается в обычную ошибку конфигурации); неизвестная переменная с префиксом `STRUCTURED_LOG_` — предупреждение (`ConfigParseResult.warnings`), не ошибка
- [x] 8.9 `--print-config` (`formatPrintConfig`): эффективная конфигурация с источником каждого значения (`cli`/`env`/`file`/`defaultValue`), секреты маскируются (`***` + источник), выход без запуска сервера; `--help`/`--version` — там же (все три — специальные исходы `ConfigParseOutcome`, а не обычные параметры)
- [x] 8.10 `ServerConfig` — иммутабельный value-объект, передаётся в `buildHandler(db, ...)`/`bootstrapAdmin(db, config, ...)` явно; глобального синглтона нет; тесты конструируют его напрямую без подмены окружения процесса (кроме 8.13, намеренно — единственный тест, которому нужен реальный отдельный процесс)
- [x] 8.11 Тест-страж полноты (`test/config/server_config_test.dart`): ручной список ожидаемых имён параметров сверяется с `serverConfigParams` (без `dart:mirrors`, которого этот пакет избегает), плюс проверка непустого описания и kebab-case имени каждого параметра, плюс сквозной вызов `ServerConfig.fromResolved` — опечатка в имени поля внутри него роняет тест уже как необработанное исключение
- [x] 8.12 Юнит-тесты на сценарии из `specs/log-server-config/spec.md`: приоритет трёх источников, пустая строка = не задано, соответствие имён CLI↔env, секрет из файла и конфликт двух форм, недоступный файл секрета, отказ без JWT-секрета (для `serve`), накопление нескольких ошибок за один запуск, порт не открывается при плохой конфигурации (config резолвится и валидируется до любого обращения к `shelf_io.serve`/`StructuredLogDatabase.open`), нечисловое/нераспознанное булево значение, неизвестный аргумент против неизвестной переменной, `create-admin` без JWT-секрета (SMTP не в Этапе 1, соответствующего сценария нет), `--print-config` не раскрывает секрет и не запускает сервер, `--help` содержит каждый параметр — `test/config/config_resolver_test.dart` (28 тестов), `test/config/server_config_test.dart` (6 тестов)
- [x] 8.13 Интеграционный тест (`test/bin/server_integration_test.dart`, тег `integration`): запуск `bin/server.dart serve` реальным OS-процессом (`Process.start`) с конфигурацией из переменных окружения и с переопределением порта аргументом — сервер поднимается и отвечает на `GET /healthz` на порту из аргумента, а не из переменной; корректно завершается по `SIGTERM` (код выхода 0). Дополнительно вручную проверен полный сценарий через реально запущенный процесс: `create-admin` → `serve` с автосозданием → вход сгенерированным паролем → `403 must_change_password` на `GET /v1/groups` → `POST /v1/auth/change-password` → тот же запрос проходит → создание группы/проекта/ключа → приём лога → запрос лога — все шаги отработали через настоящий HTTP curl к запущенному серверу

## 9. structured_log_http — клиентский sender _(Этап 1)_

- [x] 9.1 Реализовать `HttpLogOutput` (`lib/src/http_output.dart`) по паттерну `_SerializedAsyncOutput`/`AsyncFileOutput`: конструктор принимает URL сервера и секретный ключ проекта, сериализованная очередь, `catchError` на каждом шаге, `flushed`
- [x] 9.2 Батчинг по размеру/таймауту, отправка `POST /v1/logs` с `Authorization: Bearer <project-secret-key>`
- [x] 9.3 Retry с backoff на сетевых ошибках/5xx, без retry на 4xx
- [x] 9.4 Верхний предел буфера в памяти с вытеснением самых старых записей при переполнении — предел применяется ко **всем** неотправленным записям, а не только к тем, что ждут формирования батча: первая реализация выстраивала батчи в цепочку futures, и при долгой недоступности сервера память росла неограниченно через эту цепочку, пока сам буфер выглядел ограниченным. Заменено на единую очередь + насос, забирающий по `batchSize` за раз (тест на переполнение это и поймал)
- [x] 9.5 Юнит-тесты на сценарии из `specs/structured-log-http-sender/spec.md`

## 10. Интеграционное тестирование _(Этап 1 — частично, см. design.md «Delivery Phases»)_

- [x] 10.1 Сквозной интеграционный тест на реальном `HttpServer` — реализован как `test/bin/server_integration_test.dart` (файл назван иначе, чем в формулировке: он поднимает `bin/server.dart` отдельным процессом, что строго сильнее — проверяется и резолвинг конфигурации из окружения настоящего процесса). Покрыто: старт → токен → группа/проект (с квотой)/секретный ключ → приём → запрос → refresh → отзыв refresh-токена → повторное использование отозванного отклонено; плюс гейт `must_change_password`, обе формы ошибок, перекрёстное предъявление креденшелов, квоты, живой поток с catch-up, работа ограничителя частоты, диагностическое логирование и запуск purge job. Числилась незакрытой по недосмотру — обнаружено сверкой 16.3
- [ ] 10.2 _(Этап 4)_ Отдельный тест: команда с несколькими участниками получает `RoleAssignment` на проект → все текущие участники получают доступ в своём следующем токене; удаление одного участника из команды инвалидирует его уже выданный токен (401), не затрагивая токены остальных участников
- [ ] 10.3 Отдельный тест: сервер запущен с `registrationEnabled=false` (или по умолчанию) — `POST /v1/auth/register` отвечает 403
- [x] 10.4 _(Этап 3)_ Отдельный тест (блокировка): admin блокирует пользователя → его текущий access-токен отвечает 401, refresh с его refresh-токеном отклоняется (`invalid_grant`) → admin разблокирует → пользователь снова логинится штатно; admin блокирует проект → `POST /v1/logs` по его секретному ключу отвечает 403 `project_blocked`, `GET /v1/logs?project_id=...` тоже 403, `GET /v1/logs?group_id=...` возвращает записи остальных проектов группы без заблокированного → admin разблокирует проект → приём/запрос восстанавливаются без пересоздания ключей; owner той же группы получает 403 на попытку заблокировать пользователя/проект (роль `owner` для этого сценария выдаётся через `POST /v1/role-assignments`, 5.6a)
- [x] 10.5 _(Этап 3)_ Отдельный тест (самоудаление): пользователь создан через `POST /v1/users` (5.1, без email) и логинится → `DELETE /v1/users/me` с верным паролем → текущий access-токен отвечает 401, refresh отклоняется (`invalid_grant`) → повторный логин с теми же `username`/паролем отклоняется (`invalid_grant`) → `admin` не может `unblock` этого пользователя (409 `deleted_account`, `docs/api/errors.md`) → строка удалённого пользователя физически не вычищена, `username` остаётся зарезервирован (проверка через повторный `POST /v1/users` с тем же `username` → 409 `username_taken`; сценарий с самостоятельной регистрацией и `email` — 3.3, не в Этапе 3)
- [x] 10.7 _(Этап 3)_ Отдельный тест (административное удаление и единственный владелец группы): создать группу → `admin` выдаёт роль `owner` на группу пользователю U (`POST /v1/role-assignments`, 5.6a) → `admin` отправляет `DELETE /v1/users/U.id` → 409 `sole_group_owner` с этой группой в списке, `U` всё ещё может войти → `admin` выдаёт роль `owner` на ту же группу другому пользователю (5.6a) → `admin` повторяет `DELETE /v1/users/U.id` → успех без пароля `U`, его `role_assignments`/`team_members` пусты, `GET /v1/audit-log` показывает `user.deleted` с `actor` = admin, `target` = `U`; owner той же группы получает 403 на попытку удалить любого пользователя группы
- [x] 10.8 Ручной smoke test — прогнан 2026-09-15 на macOS против сборки из этой ветки, двумя сценариями. **(а)** Путь из `README.md`: `bin/server.dart serve` на пустой БД → автосозданный администратор, временный пароль перехвачен из `bootstrap.warning` (43 символа) → `GET /v1/groups` под временным паролем даёт `403 must_change_password` → смена пароля → повторный вход → группа/проект (`retention_days: 30`, `max_entries: 1000000`)/секретный ключ (префикс `slk_`) → `POST /v1/logs` (`{"accepted":1,"rejected":[]}`) → `GET /v1/logs` возвращает запись с пользовательским полем `order_id: 42` → секретный ключ на management-эндпоинте отклонён `401`. **(б)** `create-admin` при `--no-bootstrap-admin-enabled` + `HttpLogOutput`: администратор создан отдельной командой, временный пароль в stderr, дальше тот же путь до ключа; затем внешний процесс с `HttpLogOutput` (`batchSize: 3`) отправил три записи и дождался `flushed` — `GET /v1/logs` вернул все три, с сохранёнными `service` (из `bind`) и `i`. Комбинация «`HttpLogOutput` → настоящий сервер» иначе нигде не покрыта: тесты пакета говорят с локальной заглушкой `HttpServer`, а интеграционные тесты сервера ходят `curl`-ом
- [x] 10.9 Отдельный тест (основной администратор; для автосоздания при пустой базе — 34.9): `create-admin` на пустой базе создаёт первого администратора с `is_primary_admin = true` и `must_change_password = false` при заданном оператором пароле, причём сам пароль не попадает ни в stdout, ни в stderr; повторный вызов при активном администраторе отклоняется ненулевым кодом возврата и ничего не пишет в таблицу; серверный процесс, поднятый на той же базе с выключенным автосозданием, принимает вход этой учётной записью и не включает гейт `must_change_password` — `test/bin/server_integration_test.dart`. Пароль передаётся через окружение, а не флагом: секретный параметр намеренно не имеет CLI-флага вовсе (`lib/src/config/config_resolver.dart`), чтобы не оказаться в списке процессов. **Формулировка задачи разошлась с реализацией:** она предполагала, что повторный `create-admin` создаёт второго администратора без флага, тогда как `createAdmin()` намеренно отказывает, пока активный администратор существует (`design.md` decisions 12/27) — команда является путём восстановления, а не управлением пользователями; тест проверяет фактическое поведение. Создание второго администратора после деактивации первого (и то, что флаг ему не достаётся) покрыто юнит-тестом `test/auth/create_admin_test.dart`
- [x] 10.9a _(Этап 3 — требует 3.10a/3.10b/4.7 и раздел 19)_ Защита основного администратора от удаления, вторая половина исходной 10.9: `DELETE /v1/users/:id`, отправленный вторым администратором, отклоняется 403 `cannot_delete_primary_admin`, P всё ещё может войти → `DELETE /v1/users/me`, отправленный самим P, отклоняется тем же кодом → второй администратор успешно удаляется обычным образом (`DELETE /v1/users/:id` от P) → `GET /v1/audit-log` не содержит `user.deleted` для P ни в одной из отклонённых попыток
- [x] 10.10 _(по прямому запросу пользователя 15.09.2026; вне исходного объёма change)_ Сквозной e2e-пакет `packages/e2e` — первый потребитель категории `packages/`. Поднимает `bin/server.dart` настоящим процессом и прогоняет через него **всю** систему: `structured_log` → `HttpLogOutput` → сервер → `ApiClient`/репозитории admin-клиента → SSE-подписка. Покрыто: гейт `must_change_password` на свежем развёртывании и его снятие; создание группы/проекта/ключа через репозитории клиента (в том числе конверт `{"items": …}` — форма, из-за которой селектор области не мог работать ни дня); путь записи целиком, с сохранением `logger`, корреляции и произвольного контекста; живая доставка после открытия подписки; правка квоты с `null` как «без лимита» и возврат счётчиков; отказ приёма по отозванному ключу. **Зачем отдельный пакет:** все остальные наборы останавливаются на шве — серверные тесты собирают хендлер, клиентские отвечают подставным адаптером, тесты отправщика говорят с заглушкой; оба дефекта 15.09.2026 жили ровно в этих швах. `dependency_overrides` с путями прописаны в самом `pubspec.yaml`, а не оставлены `melos bootstrap`: CI идёт без melos, а hosted-`structured_log` тянул бы опубликованную версию вместо ветки. Браузерный слой сознательно **не** покрыт — это отдельное решение пользователя; класс «в браузере ведёт себя иначе» (dio на web) остаётся за юнит- и e2e-тестами невидимым, и это записано здесь, а не подразумевается
- [x] 10.11 **Найдено e2e в первый же прогон и исправлено:** сервер не останавливался по SIGTERM, пока открыта хоть одна SSE-подписка. `HttpServer.close(force: false)` ждёт завершения активных соединений, а подписка сама не завершается: закрытие broadcast'а в `_shutdown` листенера в `log_stream_route.dart` не трогало, тело ответа оставалось открытым. Для `docker stop` это десять секунд ожидания и затем SIGKILL — по базе SQLite в момент записи. Исправление: `onDone` у подписки на broadcast закрывает поток терминальным кадром `event: end` c `reason: server_shutdown`; клиент считает эту причину поводом переподключиться (перезапуск сервера — это деплой, а не конец ленты), а не концом подписки. Закреплено с двух сторон: временем остановки в `packages/e2e/test/shutdown_test.dart` и самим кадром в `test/http/routes/log_stream_route_test.dart`

## 11. structured_log_admin_client — скаффолдинг и API-клиент _(Этап 1 — частично, см. design.md «Delivery Phases»)_

- [x] 11.1 Создать `frontend/structured_log_admin_client/`: `pubspec.yaml` (зависимости `flutter` sdk, `structured_log_admin_ui` (раздел 23, path-зависимость внутри workspace), `fluent_ui`, `dio`, `retrofit`, `flutter_secure_storage`, `fpdart`, `freezed_annotation`, `json_annotation`, `cherrypick`, `flutter_bloc`; dev-зависимости `build_runner`, `freezed`, `json_serializable`, `retrofit_generator`; `structured_log` (собственное логирование приложения — decision 48 `design.md`), без зависимости на `structured_log_server`/`structured_log_http` (decision 19 `design.md`, пересмотренная decision 48 только в части `structured_log`)), `lib/main.dart` (`FluentApp`, не `MaterialApp` — decision 38), `lib/features/` — по фиче (`auth/`, `users/`, `projects/`, `teams/`, `log_browser/`, `audit/`, ... — decision 32), внутри каждой — `domain`/`application`/`infrastructure`/`presentation` (`presentation` компонует atoms/molecules/organisms `structured_log_admin_ui` в конкретные, привязанные к `Bloc` экраны — decision 39), `test/`; веб-платформа через `flutter create --platforms=web` (по аналогии с `structured_log_material_example`/`structured_log_fluent_example`); `LICENSE` скопирован; `*.g.dart`/`*.freezed.dart` добавлены в `.gitignore` пакета — создан `frontend/structured_log_admin_client/` (web через `flutter create --platforms=web`, `LICENSE`, `*.g.dart`/`*.freezed.dart` в `.gitignore`), `lib/main.dart` + `lib/app/app.dart` на `FluentApp` с пресетом `AdminTheme`. **Директории фич заводятся вместе с первым файлом, а не пустыми** (git их не хранит, и раскладка внутри фичи намеренно не фиксируется — decision 32); сейчас есть `lib/shared/` (`api`/`auth`/`config`/`di`/`logging`), фичи появятся в разделах 12–14/22. Домашний экран — заглушка до 12.1, намеренно не пустая: не поднявшееся приложение выглядело бы так же
- [x] 11.2 Добавить пакет в `packages:` корневого [melos.yaml](../../melos.yaml) и в матрицу существующего `flutter`-job'а CI (не новый job), с шагом `dart run build_runner build --delete-conflicting-outputs` перед `analyze`/`test` — пакет в `packages:` [melos.yaml](../../melos.yaml), в скоупах `test:flutter` и `generate`, в матрице `flutter`-job'а CI; шаг `build_runner` добавлен условием `if: matrix.package == ...` — он нужен только этому пакету, остальным Flutter-пакетам кодогенерация не требуется
- [x] 11.3 `ApiClient` на `dio` (`lib/shared/api/api_client.dart`): base URL сервера из конфигурации приложения, interceptor подстановки `Authorization: Bearer <access-token>`, interceptor перехвата 401 → `grant_type=refresh_token` → повтор исходного запроса ровно один раз (`specs/admin-client-auth`); поверх этого же `dio`-инстанса — кодогенерируемые `retrofit`-интерфейсы (`@RestApi()`) по одному на группу эндпоинтов (`AuthApi`/`UsersApi`/`ProjectsApi`/...), с моделями запроса/ответа как `freezed`+`json_serializable` (decision 34/37); `GET /v1/logs/stream` — вручную поверх того же `dio`-инстанса, не через `retrofit` (decision 37, реализация — раздел 22) — `ApiClient` держит **три** инстанса `dio`: основной с перехватчиком, клиент обновления без перехватчика (refresh, которому ответили 401, вошёл бы обратно в перехватчик, который его запустил) и клиент повтора. Токен-эндпоинт исключён по пути, а не только по флагу: 401 от него — это неверный пароль или израсходованный refresh-токен, и обновление в ответ на любое из двух превратило бы неудачный вход в продление сессии. Параллельные 401 делят одно обновление (иначе каждый запрос тратит refresh-токен, и при ротации все кроме первого предъявят отозванный). `retrofit`-интерфейсы — `AuthApi`/`GroupsApi`/`ProjectsApi`/`SecretKeysApi`/`LogsApi`, **только под существующие маршруты**; `LogsApi.query` выставляет всю поверхность фильтров `GET /v1/logs`, включая шесть корреляционных и произвольные `context.<ключ>` через `@Queries()` + хелпер `contextQuery()`, который ставит префикс (ключ без него сервер молча игнорирует, а не отклоняет); `GET /v1/logs/stream` в них отсутствует (decision 37, раздел 22). Ошибки — `ApiFailure` (`freezed`-sealed) через `failure_mapper.dart`, включая `429` с `Retry-After` (decision 45). `LogEntryDto` разобран вручную: сервер разливает context по верхнему уровню, и `json_serializable` не умеет «эти ключи мои, остальное сохрани»
- [x] 11.4 Хранилище токенов (`lib/shared/auth/token_storage.dart`) на `flutter_secure_storage`; тестовый мок-реализация для юнит/виджет-тестов (без реального secure storage в CI — decision 20/Risks `design.md`) — `TokenStorage` как интерфейс, `SecureTokenStorage` на `flutter_secure_storage` и `InMemoryTokenStorage` в `lib/` (не в `test/`: он нужен и виджет-тестам, и web-сборке, а из `test/` его не импортировать). Половинчатая пара токенов читается как «сессии нет»
- [x] 11.5 DI-контейнер на `cherrypick` (`lib/shared/di/`, или per-feature scope-модули — decision 35): регистрация `ApiClient`/`retrofit`-клиентов/`TokenStorage` как singleton-зависимостей, `infrastructure`-репозиториев каждой фичи как их потребителей, доступных `application`-слою (use-cases) и, через него, `presentation`-слою (`Bloc`/`Cubit`, decision 36) — ни один `Bloc`/виджет не создаёт `infrastructure`-зависимость напрямую — `lib/shared/di/app_module.dart` на `cherrypick`: `AppConfig`/`BoundLogger`/`TokenStorage`/`ApiClient` синглтонами в корневом scope, `openAppScope()` — единственная точка композиции. `BoundLogger` передаётся из `main()` экземпляром, а не провайдером: его создание устанавливает глобальный `StructlogConfiguration`, и провайдер переустанавливал бы его при каждой пересборке scope. Репозитории фич — в модулях своих subscope по мере появления фич

## 12. structured_log_admin_client — аутентификация (admin-client-auth) _(Этап 1 — частично, см. design.md «Delivery Phases»)_

- [x] 12.1 Экран логина (`username`/пароль → `grant_type=password`), сохранение полученной пары токенов, переход на основной экран — `lib/features/auth/` по слоям (`domain`/`application`/`infrastructure`/`presentation`/`di`): `AuthRepository` + реализация, use-case'ы `SignIn`/`SignOut`/`RestoreSession`, `LoginCubit` (`flutter_bloc`) и `LoginPage` по `Login.dc.html`. Ошибки входа фича разбирает сама, а не общим маппером: `POST /v1/auth/token` — единственный эндпоинт с конвертом RFC 6749 (`error`/`error_description`/`reason`), и `reason` — та самая расширяющая его деталь, по которой `email_not_verified` отличается от неверного пароля. Ссылки «Забыли пароль?» и «Зарегистрироваться» из макета не поставлены: их экраны — разделы 18 и 12.2, обоих эндпоинтов нет; ссылка в никуда хуже её отсутствия
- [ ] 12.2 _(не в Этапе 1 — требует `POST /v1/auth/register` (3.3) и экран подтверждения email (25.x))_ Экран регистрации (`POST /v1/auth/register`), понятное сообщение при 403 (регистрация отключена на сервере), переход к логину при успехе
- [x] 12.3 Действие «выйти»: `DELETE /v1/auth/token` (не блокирует очистку локальных токенов при сетевой ошибке — см. `specs/admin-client-auth`), возврат на экран логина — `SignOut` отзывает refresh-токен и **всегда** чистит локальную сессию: `DioException` при `DELETE /v1/auth/token` намеренно проглатывается, потому что оставить пользователя внутри из-за недоступной сети — худший из двух отказов
- [x] 12.4 Автоматический переход на экран логина при неудачном `refresh` (истёкший/отозванный refresh-токен) — из interceptor'а 11.3 — `SessionController` вне дерева виджетов: его дёргает перехватчик (у которого нет `BuildContext`) и он же слушается `AuthGate`. Возврат на логин сопровождается баннером «Сессия завершена», отличающимся от обычного открытия приложения
- [ ] 12.5 _(не в Этапе 1 — требует `DELETE /v1/users/me` (3.10a))_ Действие «удалить аккаунт» в настройках (только над собственной учётной записью): предупреждение о необратимости → запрос текущего пароля → `DELETE /v1/users/me`; при успехе — очистка токенов и переход на экран логина; при неверном пароле — ошибка, сессия сохраняется; при 409 (`sole_group_owner`) — показать список блокирующих групп вместо обобщённой ошибки, не выходя из сессии; при 403 (`cannot_delete_primary_admin`) — показать понятное сообщение о том, что основная учётная запись администратора не может быть удалена, вместо обобщённой ошибки, не выходя из сессии (decision 28 `design.md`)
- [x] 12.6 Виджет/юнит-тесты на сценарии из `specs/admin-client-auth/spec.md`: успешный/неверный логин, регистрация вкл/выкл (403), токены не в открытом хранилище, прозрачный refresh при 401 без видимой пользователю ошибки, переход на логин при неудачном refresh, выход очищает токены даже при недоступном сервере, удаление аккаунта (успех переводит на логин, неверный пароль не удаляет и не выходит из сессии, предупреждение показывается до отправки пароля, конфликт единственного владельца группы показывает список блокирующих групп, отказ основному администратору показывает понятное сообщение вместо обобщённой ошибки) — 51 тест в клиенте: маппинг RFC-ошибок и проглатывание отказа при выходе (`auth_repository_test`), логика формы и обратный отсчёт (`login_cubit_test`), сквозные сценарии спеки на всём собранном приложении поверх поддельного транспорта (`login_flow_test`)

## 13. structured_log_admin_client — управление ресурсами (admin-client-resource-management) _(Этап 1 — частично, см. design.md «Delivery Phases»)_

- [x] 13.1 _(Этап 3 — без поля `email`, см. design.md «Delivery Phases»)_ Экраны пользователей (список/создание, admin only, скрыт из навигации для остальных); действия «заблокировать»/«разблокировать» и «удалить учётную запись» пользователя (только admin, видно и активно только ему — 5.1); удаление — предупреждение о необратимости без запроса пароля, при 409 (`sole_group_owner`) показать список блокирующих групп вместо закрытия диалога как успешного; успешное удаление убирает пользователя из списка немедленно; действие «удалить учётную запись» скрыто или неактивно на экране пользователя с `is_primary_admin = true`, даже для вызывающего с ролью admin (decision 28 `design.md`) — отказ сервера 403 `cannot_delete_primary_admin` на случай обхода UI показывается как ошибка, а не успех
- [x] 13.2 Экраны групп (список/создание, admin only) и команд с составом (список/создание/добавление-удаление участников, owner группы/admin) — **только группы**, часть про команды не в Этапе 1 (Delivery Phases). `GroupsPage` со списком и диалогом создания; после успеха список **перечитывается**, а не дополняется локально: сервер решает, что этому вызывающему видно, и созданная администратором группа не обязана ему показываться. Клиент не прячет кнопку от не-админа — эндпоинта, сообщающего собственные роли вызывающего, в Этапе 1 нет, поэтому действие предлагается, а отказ 403 объясняется текстом; сервер остаётся источником истины, чего спека и требует
- [ ] 13.2a _(Этап 4)_ Команды с составом — серверные эндпоинты появляются в этом же этапе (5.3)
- [x] 13.3 Экраны проектов (список/создание/редактирование квоты в рамках группы, owner/admin — редактирование; user с доступом — только просмотр), отображение `entry_count`/`max_entries`/`total_bytes`/`max_bytes` вместе (5.4); индикатор состояния блокировки и действие «заблокировать»/«разблокировать» проект (только admin — 5.4, скрыто даже от owner той же группы) — `GroupDetailPage` (список проектов группы + создание) и `ProjectDetailPage` (квота с использованием, редактирование). Использование всегда рядом с лимитом (`AdminQuotaBar`), как требует сценарий «842 / 1000». **Индикатор блокировки есть, действия «заблокировать» нет:** `POST /v1/projects/:id/block` в Этапе 1 на сервере отсутствует, а индикатор виден всем с доступом на чтение. Две тонкости квоты: «без лимита» уходит как **явный `null`** (сервер отличает «не трогать» от «снять лимит» по наличию ключа, поэтому у обновления свой DTO без `includeIfNull: false`), а ответ `PATCH` не несёт счётчиков использования — их считает только `GET /v1/projects/{id}` — поэтому экран после сохранения оставляет уже показанные счётчики, иначе полосы обнулялись бы до следующей полной загрузки
- [x] 13.3a _(Этап 3)_ Действие «заблокировать»/«разблокировать» проект — серверный эндпоинт 5.4a
- [x] 13.4 Управление секретными ключами проекта: список метаданных, создание с одноразовым диалогом показа значения (копирование в буфer обмена, предупреждение о единственном показе), отзыв — значение живёт в `ProjectDetailState.revealedKey` ровно пока открыт диалог показа и сбрасывается при закрытии; список ключей его не содержит и не хранит. Отозванный ключ **остаётся** в списке с датой отзыва, а не исчезает: иначе история того, что существовало, переписывалась бы молча
- [ ] 13.5 _(Этап 4 — полная версия, см. design.md «Delivery Phases»; 13.5a — урезанный срез Этапа 3)_ UI выдачи/отзыва `RoleAssignment` (роль/область/получатель — пользователь или команда), форма ограничивает выбор ролей/областей согласно правам текущего пользователя (клиентская подсказка — сервер остаётся источником правды)
- [x] 13.5a _(Этап 3 — урезанная версия 13.5, под 4.3a/5.6a)_ UI выдачи/отзыва `RoleAssignment`: получатель — только пользователь (не команда, раздел 5.3 не в Этапе 3), доступно только вызывающему с ролью `admin`; форма не сужает выбор ролей/областей по чужим правам — в Этапе 3 выдавать роли умеет только `admin`, сужать не для кого. Поле «Область» — поиск группы/проекта по имени (`AdminSearchPicker`, `GET /v1/groups`/`GET /v1/projects` с `?name=`), не сырой `scope_id` (design.md, уточнение 17.09.2026). Отзыв — доведён до реальной функции тем же уточнением: новый `GET /v1/role-assignments` (фильтр по `subject_id` либо `scope_type`+`scope_id`, без пагинации) отдаёт текущие гранты со списком и кнопкой «Отозвать» — в `EditUserDialog` (по пользователю) и в новом разделе «Доступ» на `GroupDetailPage`/`ProjectDetailPage` (по группе/проекту, с выдачей через `GrantAccessDialog` и поиском получателя по имени — `?username=` на `GET /v1/users`, тот же приём, что `?name=` у групп/проектов). Просмотр списка по `subject_id` остаётся `admin`-only (`canManageRoleAssignments`); просмотр по `scope_type`+`scope_id` открыт также `owner` этой области (`canReadRoleAssignmentsForScope`, design.md, уточнение 17.09.2026) — выдача и отзыв как действия по-прежнему только `admin`
- [x] 13.6 Виджет/юнит-тесты на сценарии из `specs/admin-client-resource-management/spec.md` — в объёме реализованного (Delivery Phases): перечитывание списка после создания, отказ в диалоге не портит страницу за ним, пустой список отличается от упавшего, «без лимита» уходит как `null`, счётчики переживают сохранение квоты, значение ключа показывается один раз и забывается, отозванный ключ остаётся помеченным, тексты отказов и формат чисел

## 14. structured_log_admin_client — просмотр и поиск логов (admin-client-log-browser) _(Этап 1)_

- [x] 14.1 Селектор области видимости (проект/группа) — ограничен ресурсами, доступными текущему пользователю; экран списка логов недоступен без выбора — `ScopeSelector` предлагает проекты и группы рядом (роль выдаётся на любой из уровней, и ни один список не выводится из другого), заблокированные проекты помечаются, а не скрываются. Пока область не выбрана, селектор **стоит вместо списка и ни один запрос не уходит** — это закреплено тестом. Понадобился новый серверный эндпоинт: см. 5.8
- [x] 14.2 Элементы управления фильтрами, соответствующие параметрам `GET /v1/logs` (уровень/категория/logger/диапазон времени/correlation ids/`q`/`context.*`), комбинируемые в одном запросе — вся поверхность фильтров `GET /v1/logs` через `LogFilter` одним значением, поэтому «фильтры изменились» — одно сравнение, и оно же сбрасывает пагинацию. Тест проверяет, что уровень и полнотекстовый поиск уходят **одним** запросом, а не двумя
- [x] 14.3 Список результатов с постраничной подгрузкой по курсору (без дублирования записей между страницами; сброс пагинации при смене фильтров/области) — курсорная подгрузка. Страница разворачивается: сервер отвечает `ORDER BY id DESC`, а лента по спеке хронологическая, старые сверху, поэтому более ранняя страница кладётся **над** уже показанным. Сторож поколений отбрасывает ответ на отменённый запрос — два быстрых изменения фильтра иначе могли приземлиться в обратном порядке. Записи при смене области очищаются вместе с запросом, а не по его возвращении, иначе логи одной области видны под именем другой
- [x] 14.4 Детальный просмотр записи (все стандартные поля + произвольный `context`) — `LogEntryDetailPane`: стандартные поля и `context` отдельными блоками, копирование записи целиком. Живёт в клиенте, а не в `structured_log_admin_ui`: он принимает запись и решает, какие поля стандартные, а компонентам библиотеки доменные модели передавать нельзя (decision 39). В библиотеку ушла только `AdminLogEntryRow` — она принимает примитивы
- [x] 14.5 Виджет/юнит-тесты на сценарии из `specs/admin-client-log-browser/spec.md` — 13 тестов: 8 на куб (включая порядок записей, отсутствие дублей между страницами, сброс пагинации и гонку ответов) и 5 виджет-тестов на сценарии спеки. **Скриншотом экран не сверялся:** ввод текста в Flutter-канвас этим инструментом не управляется, поэтому всё за формой входа проверяется виджет-тестами и сквозным HTTP-прогоном

## 15. CI _(Этап 1)_

- [x] 15.1 Добавить в [.github/workflows/ci.yml](../../.github/workflows/ci.yml) отдельный job для `backend/structured_log_server`/`emb/structured_log_http` (матрица по пакету, `dart-lang/setup-dart`, только `ubuntu-latest`): `pub get` → (для `structured_log_server`: `dart run build_runner build --delete-conflicting-outputs`) → `format --set-exit-if-changed` → `analyze` → `test` — реализовано двумя отдельными джобами (`server` и `http-sender`), а не одной матричной: `structured_log_server` требует шага `build_runner` до `analyze`/`test`, а `structured_log_http` — чистый Dart без кодогенерации, и матрица тянула бы этот шаг за собой впустую. Обе джобы сами пишут `pubspec_overrides.yaml` с путём на `emb/structured_log`: `melos bootstrap` в CI не используется, так как `sdkPath` в `melos.yaml` указывает на FVM-SDK, которого в CI нет
- [x] 15.2 Прогнать на GitHub Actions, убедиться, что все job'ы зелёные — прогонялось многократно через `workflow_dispatch` на ветке `feature/log-server`; последний прогон на `00a54e8` зелёный целиком, 12 из 12 джоб (`test` ×3 ОС, `server`, `http-sender`, `flutter` ×7). Матрица для `frontend/structured_log_admin_client`/`structured_log_admin_ui` будет добавлена вместе с этими пакетами (разделы 11/23)

## 16. Документация и финализация _(Этап 1 — частично, см. design.md «Delivery Phases»)_

- [x] 16.1 `README.md`/`README.ru.md` для `structured_log_server` (установка, bootstrap admin, создание группы/проекта/секретного ключа, management API, HTTP-контракт приёма/запроса) — **сделано**; `structured_log_http` (установка, быстрый старт с `HttpLogOutput`) — **сделано**; `structured_log_admin_ui` и `structured_log_admin_client` — **сделано** вместе с разделами 23 и 11, билингвальными парами, как у остальных пакетов (отметка оставалась `[~]` с формулировкой «пакетов ещё нет» до 15.09.2026, когда они уже существовали). Путь из README сервера проверен целиком против запущенной сборки, тем же прогоном, что зафиксирован в 10.8 — то есть команды в нём исполняемы, а не правдоподобны
- [x] 16.2 Обновить корневые `README.md`/`README.ru.md` и разделы «Структура»/«CI» в [AGENTS.md](../../AGENTS.md) — корневые README больше не называют `structured_log_http`/`structured_log_server` скаффолдингом: у первого перечислены реальные свойства, у второго — что работает и что специфицировано, но не построено; добавлены указатель на `docs/` и быстрый старт из трёх шагов (логирование → отправка через `HttpLogOutput` → запуск сервера). В `AGENTS.md` переписаны разделы про оба пакета (там «пустой публичный API» соседствовал с описанием реализованных подсистем) и сняты ссылки на `frontend/`/`packages/` — этих директорий физически нет, git не хранит пустые. Ссылки во всех трёх файлах проверены скриптом, битых нет. Разделы «CI» в `AGENTS.md` обновлялись по мере появления джоб (`server`, `http-sender`) и уже актуальны
- [x] 16.3 `openspec-verify-change` — проведена сверка всех 17 капабилити (126 требований, 322 сценария) с кодом и тестами; отчёт ниже.

  **Покрытие.** В объёме Этапа 1 находятся 10 серверных капабилити (84 требования). Из них 67 реализованы и покрыты тестами, 3 покрыты частично (упираются в эндпоинты Этапа 2), 14 отложены вместе со своими разделами (удаление аккаунтов, самостоятельная регистрация, правила выдачи ролей, блокировка, `POST`/`PATCH /v1/users`). Остальные 7 капабилити (52 требования) целиком вне Этапа 1: аудит, восстановление пароля, подтверждение email и четыре admin-client'а — пакетов `frontend/*` ещё не существует.

  **Расхождение спеки и кода устранено правкой спеки (15.09.2026).** Требование `log-server-forced-password-change` противоречило само себе: первая половина предложения перечисляла случаи «пароль назначил сервер → флаг ставится» и не упоминала `create-admin`, а хвост утверждал безусловно, что `create-admin` флаг ставить не должна — «пароль выбирает сам человек». Сценариев было два из трёх: для `create-admin` с паролем оператора (`false`) и ни одного для запуска без пароля. Код (`mustChangePassword: Value(wasGenerated)`) вёл себя по *обоснованию* требования, а не по его букве: при сгенерированном пароле человек его как раз не выбирал, а сервер напечатал его в консоль. Поправлены обе половины предложения, переименован вводящий в заблуждение заголовок сценария («create-admin не помечает пароль временным» звучал безусловным правилом, хотя WHEN его ограничивал — именно на заголовок и опирался этот пункт при сверке) и добавлен недостающий сценарий. Код и тесты не менялись: обе ветки закреплены в `test/auth/create_admin_test.dart`

  **Проверено особо, расхождением не является.** Требование «удалённый аккаунт не может аутентифицироваться» предписывает работать через существующие проверки `is_active`, «без отдельного, нового кода проверки специально для удалённых». `passwordGrant`/`refreshTokenGrant` действительно проверяют только `is_active` — это соответствует требованию, а не нарушает его (удаление, когда появится, выставит и `is_active = false`). Отдельная проверка `deleted_at` есть только в ревалидации живого потока — её прямо предписывает задача 21.6.

  **Побочно исправлено:** задачи 4.4, 10.1 и 15.2 числились незакрытыми, хотя реализованы — отметки приведены в соответствие с кодом (см. их формулировки). Ни одна незакрытая задача Этапа 1 по серверу после этого не осталась, кроме частей, ждущих Этапа 2 (33.4, 33.5, 16.1)

  **Повторная сверка по итогам Этапа 1 (15.09.2026).** Отчёт выше снят до того, как появились пакеты `frontend/*`, и в одной фразе устарел: три клиентские капабилити из четырёх теперь реализованы в своём этап-1-объёме — `admin-client-log-browser` (6 требований из 6), `admin-client-auth` (7 из 13; остальные шесть — регистрация, подтверждение email, восстановление пароля, удаление аккаунта — вне Этапа 1) и `admin-client-resource-management` (3 из 8; пользователи, команды, роли и блокировка вне Этапа 1). `admin-client-audit-log` целиком вне этапа. Незакрытых задач Этапа 1 не осталось: 30.5 и 33.4 исчерпаны в своём объёме и размечены «Частично», 16.1 закрыта. Найдены и устранены три расхождения текста и кода: отсутствие CORS не было записано ни в одной спеке, хотя определяет способ развёртывания целиком (новое требование в `specs/log-server-api`, страж 6.7); decision 37 описывала один инстанс `dio` там, где реализация использует второй с платформенным адаптером; открытый вопрос про `EventSource` опирался на посылку, которую web-сборка опровергла. Функциональных расхождений между спеками и кодом сверка не нашла.

## 17. structured_log_server — восстановление пароля по email (log-server-password-reset) _(позже — см. design.md «Delivery Phases»)_

- [ ] 17.1 Добавить `email` (`nullable`/уникальное если задано на уровне таблицы) в `Users` (drift-таблица, миграция схемы); обязательная валидация на `POST /v1/auth/register` (400 при отсутствии), опциональное поле в `POST /v1/users`/`PATCH /v1/users/:id`
- [ ] 17.2 `PasswordResetTokens` (drift-таблица, `lib/src/storage/database.dart`): `id`, `user_id`, `token_hash`, `created_at`, `expires_at`, `used_at`
- [ ] 17.3 Интерфейс `EmailSender` (`lib/src/email/email_sender.dart`, экспортирован из barrel-файла пакета — по аналогии с `IdentityProvider`): `Future<void> send({required String to, required String subject, required String body})`
- [ ] 17.4 `SmtpEmailSender implements EmailSender` (`lib/src/email/smtp_email_sender.dart`) поверх `package:mailer`; SMTP-настройки — из `ServerConfig` (8.1)
- [ ] 17.5 `POST /v1/auth/password-reset` (JSON `{"email": "..."}"`): при существующем активном пользователе с таким `email` — инвалидировать его прежние неиспользованные токены восстановления, создать новый (высокоэнтропийная строка `Random.secure()`, хэш SHA-256, `expires_at` из конфигурации), отправить письмо через `EmailSender` (текст токена + опциональная ссылка `<passwordResetBaseUrl>?token=...`); всегда отвечать 202 с одинаковым телом, независимо от результата поиска пользователя
- [ ] 17.6 `POST /v1/auth/password-reset/confirm` (JSON `{"token": "...", "new_password": "..."}"`): найти токен по хэшу, проверить `expires_at`/`used_at IS NULL` (400 `invalid_token` иначе), обновить хэш пароля, пометить токен использованным, инвалидировать остальные неиспользованные токены пользователя, инкрементировать `token_version` (4.2); при успехе — аудит-запись `password.reset_confirmed` (актор — сам пользователь, раздел 19)
- [ ] 17.7 Юнит-тесты на сценарии из `specs/log-server-password-reset/spec.md`: одинаковый ответ на существующий/несуществующий email, истёкший/использованный/несуществующий токен отклоняются, повторный запрос инвалидирует более ранний токен, успешный confirm инвалидирует текущие access-токены (несовпадение `token_version`) и сразу позволяет войти новым паролем, вход по `username`/паролю не зависит от наличия `email`
- [ ] 17.8 Интеграционный тест (расширение `10.1` или отдельный сценарий): `POST /v1/auth/password-reset` → письмо перехвачено тестовым `EmailSender`-мок-реализацией → `POST /v1/auth/password-reset/confirm` с токеном из письма → ранее выданный access-токен отклонён (401) → вход новым паролем успешен

## 18. structured_log_admin_client — восстановление пароля (admin-client-auth) _(позже — см. design.md «Delivery Phases»)_

- [ ] 18.1 Действие «Забыли пароль?» на экране логина (12.1) → экран запроса восстановления (поле `email`, `POST /v1/auth/password-reset`, одинаковое подтверждающее сообщение независимо от ответа сервера)
- [ ] 18.2 Экран установки нового пароля (поле токена — предзаполняется из query-параметра `token` в web-сборке, иначе вводится вручную; поля нового пароля/подтверждения; `POST /v1/auth/password-reset/confirm`); понятная ошибка при 400 (`invalid_token`), предложение запросить восстановление заново
- [ ] 18.3 Обязательное поле `email` на экране регистрации (12.2, с клиентской валидацией «поле обязательно» до отправки) и, при наличии соответствующего экрана управления пользователем, отображение `email` рядом с `username`/ролями (`admin-client-resource-management`)
- [ ] 18.4 Виджет/юнит-тесты на сценарии из `specs/admin-client-auth/spec.md` (экраны «Забыли пароль?»/установки нового пароля): одинаковое сообщение независимо от существования email, успешная установка пароля предлагает войти, невалидный/истёкший токен показывает понятную ошибку

## 19. structured_log_server — аудит (log-server-audit) _(Этап 2)_

- [x] 19.1 `AuditWriter` (`lib/src/audit/audit_writer.dart`): метод записи аудит-записи (`actor_user_id`, `action`, `target_type`, `target_id`, `metadata`), принимающий текущую drift-транзакцию/`QueryExecutor`, чтобы вызывающий код мог выполнить запись внутри той же транзакции, что и само мутирующее действие
- [x] 19.2 Закрытый enum/набор констант `AuditAction` (`lib/src/audit/audit_action.dart`): `user.created`, `user.blocked`, `user.unblocked`, `user.deleted`, `group.created`, `team.created`, `team.member_added`, `team.member_removed`, `project.created`, `project.quota_updated`, `project.blocked`, `project.unblocked`, `secret_key.created`, `secret_key.revoked`, `role_assignment.created`, `role_assignment.revoked`, `password.reset_confirmed`, `email.verified`, `user.updated`, `password.changed`, `auth.login_succeeded`, `auth.login_failed`, `auth.logged_out`, `auth.throttled` (29.1), `audit.purged` (31.6) — **все 25 значений сразу**, ровно как в требовании 1 `specs/log-server-audit`. Набор, дополняемый по частям, обессмысливает то, ради чего он закрытый: на него опираются проверка полноты классификации (31.3) и страж совпадения строк со спекой. Девятнадцать из двадцати пяти на момент Этапа 2 не имеют вызывающего кода — их эндпоинтов ещё нет (см. design.md «Delivery Phases», Этап 2, *Частично*), и это не то же самое, что отсутствовать в наборе
- [x] 19.3 Прошить вызовы `AuditWriter` во все мутирующие эндпоинты по месту (3.3, 3.10a, 3.10b, 5.1–5.6, 17.6, 26.4 — уже отмечено в соответствующих задачах), внутри той же транзакции, что и сама операция — откат операции (включая отказ 3.10 по причине `sole_group_owner`) не должен оставлять аудит-запись **Частично:** проведены шесть действий, чьи эндпоинты существуют — `group.created`, `project.created`, `project.quota_updated`, `secret_key.created`, `secret_key.revoked`, `password.changed`. Остальные девятнадцать значений закрытого набора подключит тот этап, который вводит свой эндпоинт (design.md «Delivery Phases», Этап 2, *Частично*). Два маршрута при этом обзавелись транзакцией, которой у них не было: создание группы было голым вставом, создание ключа — вставом и чтением
- [x] 19.4 `GET /v1/audit-log` (`lib/src/http/routes/audit_log_route.dart`): `admin` only, фильтры `actor_user_id`/`action`/`target_type`/`target_id`/`from`/`to`, keyset-пагинация по `id` (тот же паттерн, что `query.dart` из 2.4)
- [x] 19.5 Юнит-тесты на сценарии из `specs/log-server-audit/spec.md`: создание аудит-записи при каждом типе действия из 19.2, отсутствие аудит-записи при отклонённом/откатившемся действии, `actor_user_id` самостоятельной регистрации равен самому созданному пользователю, отсутствие записей о `POST`/`GET /v1/logs`, 403 для не-admin на `GET /v1/audit-log`, фильтрация по действию/цели, пагинация курсором без пересечения страниц
- [x] 19.6 Интеграционный тест (расширение `10.1`/`10.4` или отдельный сценарий): выполнить несколько разнотипных мутирующих действий (создание роли, блокировка проекта, отзыв ключа) → `GET /v1/audit-log` как admin показывает все три записи с корректными `actor`/`target`/`metadata`; owner получает 403 на тот же запрос

## 20. structured_log_admin_client — просмотр аудита (admin-client-audit-log) _(Этап 2)_

- [x] 20.1 Экран аудита (список), видимый и доступный только admin, скрыт из навигации для остальных ролей — `lib/features/audit/presentation/audit_page.dart`; пункт навигации показывается по claim `roles` access-токена (`shared/auth/access_token_claims.dart` → `AuthRepository.isGlobalAdmin()` → `IsGlobalAdmin`). Читается непроверенная подпись, и это законно ровно потому, что решает, **что предложить**, а не что разрешить: источник истины — 403 сервера, который экран рисует отказом. Следствие записано в коде: отозванная в середине сессии роль оставит пункт до обновления токена, а экран за ним начнёт отвечать 403 сразу. `home_shell.dart` при этом перестроен — `AdminAppShell.selectedIndex` считает по всем секциям подряд, так что условный пункт сдвигал бы следующие; теперь навигация это один список записей, из которого выводятся и секции, и индекс, а `content` — исчерпывающий `switch`
- [x] 20.2 Элементы управления фильтрами, соответствующие параметрам `GET /v1/audit-log` (действие/тип цели/идентификатор цели/инициатор/диапазон времени), комбинируемые в одном запросе — все пять в `AdminFilterBar`; действие и тип цели — меню по закрытым наборам (`AuditAction`/`AuditTargetType` в `shared/api/dto/audit_dto.dart`), а не текстовые поля: сервер отказывает на неизвестное значение с 400, и поле, способное его содержать, превращало бы опечатку в неудачный запрос. Диапазон дат — новый `AdminDateRangeField` (23.x). Фильтр «записи без инициатора» намеренно не заведён: `AuditQuery` на сервере это умеет, но маршрут параметр наружу не отдаёт, и клиент, отправивший игнорируемый параметр, показал бы больше запрошенного, отрапортовав, что сузил
- [x] 20.3 Список результатов с постраничной подгрузкой по курсору; сброс пагинации при смене фильтров — `AuditCubit.loadMore()` дозаписывает страницу, `applyFilter()` всегда начинает с первой. Курсор принадлежит породившему его запросу, поэтому `QueryAuditLog.more()` принимает его вместе с фильтром: сервер переприменяет фильтр на каждой странице, и курсор, унесённый на другой фильтр, листал бы вопрос, которого никто не задавал
- [x] 20.4 Отображение записи: инициатор, действие, тип/идентификатор цели, время, `metadata` в структурированном (не сыром JSON) виде — колоночный `AdminTable` (23.x) по артборду `AuditLog.dc.html`; `metadata` разбирается в пары `ключ: значение` (`audit_metadata_view.dart`), причём `before`/`after` квоты сводятся в одну строку «было → стало» по изменившимся ключам, а `null` внутри квоты читается как «без лимита», не как пропуск. В теге действия стоит **строка провода**, как нарисовано на артборде: это то, по чему читатель фильтрует и что документирует API, а журнал с расходящимися отображаемым и запрашиваемым словарями превращает это в два разных разговора; русская подпись — тултип и текст в меню фильтра. Инициатор и цель показаны идентификаторами: эндпоинта, превращающего id в имя, на этом этапе нет
- [x] 20.5 Виджет/юнит-тесты на сценарии из `specs/admin-client-audit-log/spec.md` — `test/features/audit/audit_test.dart` (фильтр на проводе, конвертация в UTC, отказ как `Left`, резолвинг сабскоупа — проверен мутацией) и `audit_screen_test.dart` (словарь действий, инициатор/цель, разбор `metadata`, склонение срока, кубит, экран). Плюс `test/integration/audit_integration_test.dart` поверх мока сетевого слоя — он один покрывает то, чего виджет-тесты фичи не видят: что секция вообще вшита в оболочку, что claim роли доходит до навигации, что за именем несуществующей учётки **не уходит запрос** и что непредложенная секция не запрашивается

## 21. structured_log_server — живой поток логов (log-server-live-stream) _(Этап 1)_

- [x] 21.1 Выделить `LogFilter` (`lib/src/storage/log_filter.dart`) — единая модель фильтра (уровень/категория/logger/поля корреляции/`q`/`context.*`), из которой строится и SQL `WHERE` (переиспользует/рефакторит `query.dart` из 2.4), и предикат `bool matches(LogEntryRow entry)` для проверки одной уже загруженной записи в памяти — без двух параллельных реализаций фильтрации (decision 29 `design.md`)
- [x] 21.2 Глобальный broadcast (`lib/src/live/log_broadcast.dart`): `StreamController<LogEntryRow>.broadcast()` на весь процесс; публикация каждой успешно вставленной (прошедшей валидацию и квоту) записи батча сразу после коммита транзакции приёма (интеграция с 6.1/7.2)
- [x] 21.3 `GET /v1/logs/stream` (`lib/src/http/routes/log_stream_route.dart`): JWT-auth + authorization middleware (переиспользует раздел 4), обязательный `project_id`/`group_id`, парсинг тех же фильтров, что 6.2, через `LogFilter` (21.1); при `project_id`/`group_id` заблокированного проекта — то же поведение, что 6.2 (403 `project_blocked` при прямом project_id / молчаливое исключение при group_id), проверяемое на каждое транслируемое событие (point-lookup `is_blocked`), не только при подписке
- [x] 21.4 SSE-framing (`lib/src/http/sse.dart`): кодирование `id:`/`event:`/`data:`-кадров и heartbeat-комментариев в `Stream<List<int>>` тела ответа (`Content-Type: text/event-stream`)
- [x] 21.5 Catch-up по `since_id` (в `log_stream_route.dart`): подписка на broadcast с буферизацией входящих событий → запрос записей с `id > since_id` через `LogStore` (2.4) → отдача их как SSE-событий → сброс буфера подписки с отбрасыванием уже отданных `id` → продолжение живого потока без гонки/дублей (decision 29 `design.md`)
- [x] 21.6 Heartbeat-таймер (интервал из `ServerConfig`, 8.1) с периодической ревалидацией `token_version`/`is_active`/`deleted_at` пользователя и блокировки проекта(ов) текущей подписки; закрытие соединения терминальным SSE-событием (`event: end`, `reason: token_revoked`/`project_blocked`) при потере валидности
- [x] 21.7 Юнит-тесты на сценарии из `specs/log-server-live-stream/spec.md`: авторизация до открытия потока (успех/403/404), фильтрация событий (проходит/не проходит), блокировка проекта (прямая подписка отклонена, групповая — молча исключает, блокировка во время активной подписки закрывает её), catch-up без потерь/дублей, поведение без `since_id`, heartbeat не рвёт валидное соединение, ревалидация рвёт соединение при отзыве прав
- [x] 21.8 Интеграционный тест (`test/bin/server_integration_test.dart`, второй сценарий): поднимает `bin/server.dart` как процесс, логинится bootstrap-админом (смена временного пароля), создаёт группу/проект/ключ, открывает `GET /v1/logs/stream?project_id=P` → `POST /v1/logs` → событие доставлено по подписке → подписка обрывается, приходят ещё записи → переподключение с `since_id` доставляет пропущенные ровно один раз. **Последняя нога — блокировка `P` закрывает активную подписку — не покрыта здесь:** для неё нужен `POST /v1/projects/:id/block` (раздел 5, не реализован); на уровне хендлера она покрыта в `test/http/routes/log_stream_route_test.dart`, где блокировка ставится напрямую в БД. Именно этот тест выявил, что `shelf_io` по умолчанию буферизует потоковое тело (`shelf.io.buffer_output`) — handler-тесты этого показать не могут

## 22. structured_log_admin_client — лента логов как история чата (admin-client-log-browser) _(Этап 1)_

- [x] 22.1 Клиент живого потока (`lib/features/log_browser/infrastructure/log_stream_client.dart`): потоковый `GET` через `dio` (`ResponseType.stream`, тот же interceptor подстановки `Authorization`/refresh, что 11.3) на `/v1/logs/stream`, вручную, не через `retrofit` (decision 37) — построчный разбор SSE-кадров (`id:`/`event:`/`data:`, heartbeat-комментарии), реакция на терминальное событие (переподключение через тот же путь, что обычный 401 — 11.3/12.4) и на разрыв соединения (переподключение с backoff, `since_id` = `id` последней отображённой записи) — разбор кадров вынесен в публичную `parseSseFrames()` (кадр, разорванный между TCP-чанками, `data:` с переносами строк, многобайтный символ на границе чанка — всё это проверяется отдельно от сокета). Переподключение живёт здесь, а не в блоке: обрыв, простой, 5xx и `end: token_revoked` для экрана — одно и то же, пауза в пару секунд, а `since_id` закрывает разрыв на сервере. Наружу отдаются только исходы, которые переподключением не лечатся: `end: project_blocked` и отказ открыть подписку (403/401 после исчерпанного refresh). **`since_id` — id последней *доставленной* записи, а не отображённой:** во время паузы записи доставлены, но не показаны, и переподключение по отображённой прислало бы их повторно. Неизвестная причина в `end` считается терминальной — причина, которой клиент не знает, это причина, которую он не может объявить обработанной. Простой дольше `idleTimeout` (90 с против серверных ~30 с heartbeat) считается мёртвым соединением: сокет об этом не сообщает. **На web dio не умеет стримить вовсе** — обнаружено проверкой на поднятом стенде 15.09.2026, юнит-тестами не ловится: они идут на Dart VM с подставным адаптером. Браузерный адаптер dio ставит `xhr.responseType = 'arraybuffer'` и отдаёт тело в `onLoad`, то есть когда ответ закончился; для подписки, которая заканчиваться не должна, это «никогда» — соединение открывалось, отвечало 200 и молчало. Это делает посылку decision 29 неверной: там `EventSource` отклонён в том числе потому, что «единственный потребитель — `dio`», а `dio` на web не подходит ровно так же. Решение — `fetch` с чтением `ReadableStream`: он умеет и произвольные заголовки, и потоковое тело, поэтому контракт сервера (`Authorization: Bearer`) не меняется и токен не уезжает в URL. Подменён **только адаптер** (`shared/api/streaming/streaming_adapter_web.dart`, условный импорт; вне web — `null`, штатный `dart:io`-адаптер уже стримит), а не транспорт целиком: `LogStreamClient` и перехватчик остались как были. Адаптер стоит на отдельном `ApiClient.streamDio`, который несёт **тот же объект** `AuthInterceptor`, что и основной инстанс — иначе «одно обновление токена в полёте» перестало бы быть правдой. Проверено на стенде: живое дописывание, пауза с буфером и возобновление
- [x] 22.2 `LogFeedBloc` (`lib/features/log_browser/presentation/log_feed_bloc.dart`, `flutter_bloc` — decision 36): состояния как `freezed`-объединение (`Following`/`ScrolledUp`/`Paused`, decision 34) — реализуют диаграмму переходов decision 30; открытие подписки (22.1) с `since_id` сразу вслед за загрузкой первой страницы (14.x); автослежение — дописывание новых событий в конец и автопрокрутка, пока пользователь у нижнего края; при прокрутке выше — накопление непоказанных записей и счётчик для индикатора «N новых событий», без принудительного изменения прокрутки — **отступление от буквы, по решению пользователя 15.09.2026:** объединение из трёх вариантов — это `LogFeedMode` (`Following`/`ScrolledUp(unseen)`/`Paused(buffered, overflowed)`), *поле* внутри `LogFeedState`, а сам стейт остался data-классом. Причина та же, что была записана для `LogBrowserState` в разделе 14: «есть область», «грузится страница», «выбрана запись» — одновременные условия, а не альтернативы, и плоское объединение размножило бы `entries`/`nextCursor`/`selectedEntryId` по каждому варианту. Взаимоисключающий здесь только режим слежения, и он объединением и стал. `LogBrowserCubit` раздела 14 **заменён** этим блоком, а не дополнен им: сброс страницы + закрытие подписки + открытие новой должен быть одним переходом внутри одного объекта, иначе проводка уезжает в виджет — то самое место, где в этом репозитории её трижды забывали. Асинхронны только `_reload`/`_loadOlder`, остальные обработчики синхронны, поэтому переходы decision 30 не переплетаются и event transformer не нужен; всё в полёте помечено `_generation`
- [x] 22.3 Подгрузка более ранних записей по прокрутке к верхнему краю списка (замена/уточнение прежнего явного действия «подгрузить следующую страницу» из более ранней ревизии этого экрана — курсор `GET /v1/logs` тот же, 14.3), добавление подгруженной страницы сверху без смещения уже показанного внизу — список рисуется `reverse: true`, и это ключевое решение экрана: индекс 0 — низ (самая свежая запись), живой край — смещение 0, самая старая запись — `maxScrollExtent`. Отсюда бесплатно получаются оба требования: дописанная снизу запись не двигает вьюпорт прокрученного вверх читателя, и подгруженная в дальний конец страница тоже. Прямой список требовал бы ручной коррекции смещения после каждой вставки — источник «список прыгнул». Страница дедуплицируется по `id`: подписка могла успеть дописать запись, которую та же страница принесёт ещё раз
- [x] 22.4 Действие «пауза»/«возобновить» (независимое от прокрутки): заморозка видимого списка на паузе при открытой в фоне подписке (22.1 не закрывается), ограниченный по размеру буфер поступающих во время паузы событий; возобновление — сброс буфера в список и прокрутка к концу, либо (если буфер переполнился) перезагрузка первой страницы (14.x) и новая подписка с этого места вместо показа неполного буфера — предел буфера 500 записей (десять страниц), по тому же принципу, что верхний предел буфера `HttpLogOutput`; вытесняются самые старые, и факт вытеснения — это `overflowed` в `FeedPaused`, по которому возобновление уходит в перезагрузку. Явная пауза старше позиции прокрутки: возврат к нижнему краю её не снимает
- [x] 22.5 Сброс пагинации (14.3) и переоткрытие подписки (22.1) при смене фильтров/области — закрытие текущей подписки перед открытием новой с обновлёнными параметрами — параметры запроса для списка и для подписки сверяются тестом (`test/features/log_browser/log_query_params_test.dart`): список уходит через сгенерированный `LogsApi`, поток — через `logQueryParameters()`, и разойтись им мешает только этот тест. Отставка подписки синхронна (`_invalidate()`), а `cancel()` не ожидается: старую подписку обезвреживает `_generation`, а не завершение teardown — и `await` перед `emit` кладёт перерисовку на оборот позже, чем её ждёт `pumpAndSettle` в виджет-тесте
- [x] 22.6 Виджет/юнит-тесты на сценарии из `specs/admin-client-log-browser/spec.md` (лента как история чата): автослежение у нижнего края, индикатор новых событий и возврат к живому краю при прокрутке вверх, подгрузка истории по прокрутке к верху без дублей, пауза замораживает список, возобновление показывает пропущенное, переполнение буфера паузы приводит к перезагрузке, смена фильтров закрывает старую подписку и не смешивает её события с новой — 98 тестов в пакете (22 на `LogFeedBloc`, 10 на SSE-клиент, 3 на совпадение параметров, 10 на экран). **Две ловушки `testWidgets`, пойманные здесь и стоившие больше всего времени:** `Bloc`, созданный в `setUp`, не обрабатывает события внутри виджет-теста (его контроллер остаётся в зоне снаружи), поэтому блок строится в теле теста; и `Bloc.close()` внутри `testWidgets` не завершается никогда (воспроизводится на двухстрочном блоке — это харнесс, не наш код), поэтому в `tearDown` он не ожидается. С `Cubit` ни того, ни другого не было — обе всплыли ровно при переходе на `Bloc`

**Сверх задач раздела, по канвасу.** `LogBrowser.dc.html` рисует все четыре состояния ленты (`live`/`unseen`/`paused`/`overflow`) и строку загрузки более ранних записей, поэтому в `structured_log_admin_ui` добавлены `AdminLivePill`/`AdminLiveDot` (molecule) и `AdminFeedStatusStrip` (organism, пять именованных конструкторов — полосы взаимоисключающие, «что лента делает сейчас» это один факт), плюс токены `liveFg`/`liveBg` (своя зелёная, не `success*`: артборд задаёт её инлайном и на шаг ярче статусной) и `AdminSpacing.x7`. Шапка экрана приведена к канвасу: заголовок «Логи», пилюля области и кнопка «Изменить область» — последняя раньше была в тулбаре и вызывала `select(null)`, то есть снимала выделение записи вместо смены области; теперь это `LogFeedEvent.scopeCleared()`. **Одна полоса вне канваса:** подписка, закрытая сервером (`end: project_blocked`), рисуется вариантом `stalled` с предложением перезагрузить — канвас такого состояния не знает, а молча остановившаяся лента неотличима от тихого проекта

## 23. structured_log_admin_ui — библиотека UI-компонентов (atoms/molecules/organisms) _(Этап 1)_

Скаффолдинг этого раздела выполняется до или одновременно с 11.1, не после — экраны `structured_log_admin_client` с самого начала компонуются из этих виджетов, а не заводят временные локальные копии для последующего переноса (decision 39/Migration Plan `design.md`).

- [x] 23.1 Создать `frontend/structured_log_admin_ui/`: `pubspec.yaml` (зависимости — только `flutter` sdk и `fluent_ui`, без `fpdart`/`freezed`/`cherrypick`/`flutter_bloc`/`dio`/`retrofit` и без зависимости на `structured_log_admin_client` — decision 39 `design.md`), `lib/structured_log_admin_ui.dart` (barrel, реэкспортирующий `atoms.dart`/`molecules.dart`/`organisms.dart`), `lib/src/atoms/`, `lib/src/molecules/`, `lib/src/organisms/`, `test/`; `LICENSE` скопирован — создан `frontend/structured_log_admin_ui/` (`publish_to: none`, зависимости — только `flutter` sdk и `fluent_ui`), barrel реэкспортирует `atoms.dart`/`molecules.dart`/`organisms.dart` и `tokens.dart`
- [x] 23.2 Добавить пакет в `packages:` корневого [melos.yaml](../../melos.yaml) и в матрицу существующего `flutter`-job'а CI (не новый job); `structured_log_admin_client` (11.1) получает на него path-зависимость — в `packages:` [melos.yaml](../../melos.yaml), в скоуп `test:flutter` и в матрицу `flutter`-job'а CI; path-зависимость от `structured_log_admin_client` появится вместе с ним (11.1)
- [x] 23.3 Design tokens (`lib/src/tokens/`): цветовая палитра (включая собственный маппинг уровня лога — `LogLevelColors`, продублированный, не импортированный из `structured_log_material`/`fluent`, decision 39), типографика, отступы — используются atoms, при необходимости экспортируются как `FluentThemeData`-пресет для `structured_log_admin_client`'s `main.dart` — `lib/src/tokens/`: `AdminColors` (палитра `--fl-*` из канваса дословно), `AdminTypography`, `AdminSpacing`/`AdminRadius`/`AdminSizes`, `AdminLogLevel`+`AdminLogLevelColors` (собственная копия, не импорт), `AdminTheme` — пресет `FluentThemeData` для `main.dart` клиента. Фон бейджа уровня выводится правилом «цвет поверх поверхности при alpha 0.16», которое воспроизводит все четыре значения канваса точно (тест `test/tokens/admin_log_level_test.dart`) — поэтому `trace`/`critical`, которых нет ни на одном артборде, согласованы с остальными. **Тёмная тема выведена, а не сверена:** канвас светлый, тёмных артбордов нет
- [x] 23.4 Atoms: базовый набор для словаря admin-клиента — `LogLevelBadge`, стилизованные текстовые/кнопочные варианты поверх `fluent_ui`-примитивов, индикатор загрузки, пустое состояние — каждый принимает только примитивные/enum-параметры и колбэки — `AdminLogLevelBadge`, `AdminButton` (accent/standard/tonal поверх `Button` из `fluent_ui`), `AdminTag`, `AdminLoadingIndicator`, `AdminEmptyState`; все принимают только примитивы/enum/колбэки
- [x] 23.5 Molecules: сочетания atoms — поле поиска с иконкой/очисткой, чип фильтра, строка «ключ: значение» (для отображения квоты — `entry_count`/`max_entries`), переключатель с подписью, индикатор состояния блокировки — `AdminSearchField` (поле с иконкой и очисткой; очистка — не из канваса, в нём только состояние покоя), `AdminFilterChip`, `AdminKeyValueRow`, `AdminQuotaBar` (использование и лимит одной величиной, «842 / 1 000»; при незаданной квоте дорожка не рисуется), `AdminStatusTag` (neutral/success/warning/error), `AdminLabeledToggle` (переключателя в канвасе нет — оформлен по соседним строкам форм). Позже, вместе с разделом 12, добавлены ещё две — `AdminTextField` (поле с подписью, местом под ссылку на строке подписи и текстом ошибки) и `AdminBanner` (info/warning/error): экран логина состоит из них, а по decision 39 такие виджеты живут в библиотеке, а не в экране
- [x] 23.6 Organisms: самодостаточные секции — строка списка ресурса (пользователь/проект/группа) с действиями, панель фильтров (комбинация molecules), каркас диалога подтверждения (предупреждение + слот для полей + кнопки — без встроенной логики отправки), каркас навигации/app shell; допустимо эфемерное локальное UI-состояние (`StatefulWidget`/`ValueNotifier`), не `flutter_bloc` — `AdminAppShell` (nav-панель 252px с секциями, рельсом активного пункта и блоком учётной записи; плоский индекс по всем секциям), `AdminResourceRow`, `AdminFilterBar` (перенос строк — из канваса, `flex-wrap:wrap`), `AdminConfirmDialog` (каркас: предупреждение, слот для полей, две кнопки; ничего не отправляет и не закрывает себя, `onConfirm: null` — способ выразить «поле ещё не заполнено»). По ходу `AdminButton` разделён на variant (accent/standard/danger) и size (tonal/toolbar/dialog): в канвасе три заливки и три высоты, а прежний enum их смешивал
- [x] 23.7 `example/`: приложение-галерея, отображающая каждый atom/molecule/organism (по аналогии с `structured_log_material_example`/`structured_log_fluent_example`) — добавляется в `melos.yaml` — `example/` (`structured_log_admin_ui_example` в `melos.yaml` и в матрице CI), web-галерея всех компонентов с переключателем тёмной темы, запускается `flutter run -d chrome`
- [x] 23.8 Виджет-тесты на каждый atom/molecule/organism (рендеринг с разными параметрами, колбэки вызываются по ожидаемым взаимодействиям); тест на отсутствие импорта `structured_log_admin_client`-специфичного кода (проверка `pubspec.yaml`/факта отсутствия такой зависимости — граница decision 39 держится компилятором, тест документирует это намерение) — 48 тестов в пакете плюс 2 в галерее: по каждому atom/molecule/organism (рендер с разными параметрами, колбэки), сверка токенов уровня с канвасом и `test/package_boundary_test.dart` — запрещённых зависимостей нет в манифесте, импорта клиента нет ни в одном файле
- [x] 23.9 _(отдельной задачей, по решению пользователя 15.09.2026)_ Адаптивные состояния компонентов: канвас нарисован только в десктопной ширине (1160/1440 px, диалоги фиксированы), узких артбордов в нём нет, поэтому раздел 23 сделан строго по макетам, без адаптивности, а токены брейкпоинтов не заводились, чтобы не остаться без единого потребителя. Задача: дорисовать в канвасе узкие состояния ключевых экранов (свёрнутый в рельс nav-pane, свёрнутый master-detail, перенос панели фильтров), затем реализовать их в organisms через `LayoutBuilder` по собственной ширине виджета, а не по ширине окна — как это уже сделано в скинах `emb/` (`_masterDetailBreakpoint`/`_toolbarBreakpoint`) — **сделано в обеих половинах.** В канвас добавлены два артборда: `GroupsNarrow.dc.html` (рельс навигации) и `LogBrowserNarrow.dc.html` (свёрнутый master-detail и перенос фильтров, с tweak-параметром `view: list | detail`); оба проверены статическим прогоном `renderVals()` во всех состояниях до публикации. Заведены токены `AdminBreakpoints`: `navRail = 900` по собственной ширине оболочки и `masterDetail = 700` по собственной ширине контентной панели — числа взяты из нарисованных ширин (252 пейн + 340 список + панель деталей, которую стоит читать), а не из каталога устройств. Реализовано: `AdminAppShell` сворачивает nav-pane в рельс 56 px (иконки с подсказками, без подписей, заголовков секций и имени аккаунта — аватар остаётся кнопкой); `AdminLogEntryRow` получил `showsDisclosure` (шеврон, как у `structured_log_cupertino`) — он осмыслен только там, где тап уводит на отдельный экран; `LogBrowserPage` ниже брейкпоинта показывает ленту целиком, а выбранная запись заменяет её экраном с кнопкой «К ленте». **Попутно найдено узким тестом:** шапка экрана логов переполнялась на 640 px — теперь пилюля области сжимается с многоточием, а «Изменить область» становится иконкой с подсказкой. Панель фильтров адаптировать не пришлось: `AdminFilterBar` уже переносится тем же `flex-wrap`, что и в широком макете. Тесты разведены по ширинам: существующие тесты оболочки явно просят широкий вьюпорт (дефолтные 800 теперь ниже брейкпоинта — это поймалось сразу), плюс новые на рельс, на индекс элемента рельса и на свёрнутый master-detail в обе стороны

## 24. structured_log_server — подтверждение email (log-server-email-verification) _(позже — см. design.md «Delivery Phases»)_

- [ ] 24.1 `EmailVerificationTokens` (drift-таблица, `lib/src/storage/database.dart`, раздел 2.1) — уже отмечено в 2.1; `email_verified_at` на `Users` — уже отмечено в 2.1
- [ ] 24.2 Общая функция выпуска токена подтверждения (`lib/src/auth/issue_email_verification.dart`), вызываемая и из 3.3 (регистрация), и из 5.1 (`POST /v1/users` с непустым `email`): инвалидация прежних неиспользованных токенов подтверждения этого пользователя (если есть), создание нового (`Random.secure()`, хэш SHA-256, `expires_at` из конфигурации — 8.1), отправка через `EmailSender` (17.3/17.4, интерфейс не создаётся заново) — письмо содержит сырой токен для ручного ввода и опциональную ссылку `<emailVerificationBaseUrl>?token=...`
- [ ] 24.3 `POST /v1/auth/verify-email` (JSON `{"token": "..."}"`, не form-encoded): поиск токена по хэшу, проверка `expires_at`/`used_at IS NULL` (400 `invalid_token` иначе), при успехе — `User.email_verified_at = now()`, пометка токена использованным, инвалидация остальных неиспользованных токенов подтверждения этого пользователя; аудит-запись `email.verified` (актор — сам пользователь, раздел 19)
- [ ] 24.4 `POST /v1/auth/verify-email/resend` (JSON `{"email": "..."}"`): всегда 202 с одинаковым телом независимо от существования/состояния email; при найденном активном пользователе с этим `email` и `email_verified_at IS NULL` — вызов общей функции 24.2
- [ ] 24.5 Проверка `email IS NULL OR email_verified_at IS NOT NULL` в `grant_type=password` (3.5) — уже отмечено в 3.5; расширение RFC-ошибки полем `reason: "email_not_verified"`
- [ ] 24.6 Юнит-тесты на сценарии из `specs/log-server-email-verification/spec.md`: вход отклонён для неподтверждённого email (с `reason` в теле ошибки), разрешён после подтверждения, учётная запись без email не блокируется, `grant_type=refresh_token` не проверяет `email_verified_at` повторно, токен выпускается при регистрации и при `POST /v1/users` с email (и не выпускается без email), валидный/невалидный/повторно использованный токен подтверждения, `resend` — одинаковый ответ независимо от состояния и инвалидация более раннего токена
- [ ] 24.7 Интеграционный тест (расширение `10.1` или отдельный сценарий): `POST /v1/auth/register` → `grant_type=password` с верным паролем отклоняется (`invalid_grant`, `reason: email_not_verified`) → письмо перехвачено тестовым `EmailSender` → `POST /v1/auth/verify-email` с токеном из письма → повторный `grant_type=password` успешен → `GET /v1/audit-log` показывает `email.verified` с `actor`/`target`, равными этому пользователю

## 25. structured_log_admin_client — подтверждение email (admin-client-auth) _(позже — см. design.md «Delivery Phases»)_

- [ ] 25.1 Экран «подтвердите email» после успешной регистрации (12.2) — вместо немедленного перехода к логину; действие «отправить письмо ещё раз» → `POST /v1/auth/verify-email/resend`, одинаковое подтверждающее сообщение независимо от ответа сервера
- [ ] 25.2 Экран подтверждения email (поле токена — предзаполняется из query-параметра `token` в web-сборке, иначе вводится вручную; `POST /v1/auth/verify-email`); успех — переход на экран логина; понятная ошибка при 400 (`invalid_token`) с предложением отправить письмо повторно
- [ ] 25.3 Экран логина (12.1): при `invalid_grant` с `reason: "email_not_verified"` — специфичное сообщение и действие «отправить письмо ещё раз» (переиспользует 25.1), вместо обобщённого сообщения о неверных кредах
- [ ] 25.4 Виджет/юнит-тесты на сценарии из `specs/admin-client-auth/spec.md` (подтверждение email): регистрация показывает экран подтверждения вместо логина, повторная отправка показывает подтверждение независимо от состояния, успешное подтверждение переводит на логин, невалидный/истёкший токен показывает понятную ошибку, неподтверждённый email на логине объяснён отдельно от неверных кредов

## 26. structured_log_server — редактирование пользователя и временный пароль (log-server-forced-password-change) _(Этап 1 — частично, см. design.md «Delivery Phases»)_

- [x] 26.1 `must_change_password` на `Users` (drift-таблица, раздел 2.1) — уже отмечено в 2.1
- [x] 26.2 _(Этап 3 — без поля `email`)_ `PATCH /v1/users/:id` (`lib/src/http/routes/users_route.dart`, admin only — 4.5-подобная проверка, раздел 4): частичное обновление `display_name`/`password` (без `email` — раздел 24 не в объёме этапа, см. design.md «Delivery Phases»); смена `password` → `must_change_password = true` + отзыв всех `refresh_tokens` цели (тот же вызов, что 4.5/18) + инкремент `token_version` (4.2); аудит `user.updated` (раздел 19) с `metadata`, перечисляющей изменённые поля, без значения пароля в любой форме
- [x] 26.3 _(Этап 3)_ Установка `must_change_password = true` в `POST /v1/users` (5.1) при создании — до Этапа 3 только auto-bootstrap (раздел 34) и `create-admin` (раздел 8.3) ставили пользователя с временным паролем
- [x] 26.4 Middleware проверки `must_change_password` (`lib/src/http/must_change_password_middleware.dart`): после auth-middleware (3.8), до authorization; читает `VerifiedIdentity.mustChangePassword` (объединено с point-lookup'ом `token_version` внутри `LocalIdentityProvider`, без отдельного запроса); при `true` — пропускает только `POST /v1/auth/change-password` (единственный путь, который реально существует в Этапе 1 из полного списка исключений — `DELETE /v1/users/me` не в Этапе 1, `POST`/`DELETE /v1/auth/token` не JWT-эндпоинты вовсе и никогда не стоят за этим middleware); для остальных путей — 403 `must_change_password`, общий JSON-конверт
- [x] 26.5 `POST /v1/auth/change-password` (JSON `{"current_password": "...", "new_password": "..."}"`, доступен любой аутентифицированной роли только над собой) — `lib/src/http/routes/change_password_route.dart`: проверка `current_password` (401 `invalid_grant` при неверном), при успехе — обновление `password_hash`, инкремент `token_version` (4.2), `must_change_password = false`; ответ `200 {}` (`docs/api/http-api.md`). Аудит `password.changed` не пишется — раздел 19 не в Этапе 1
- [x] 26.6 Юнит-тесты на сценарии из `specs/log-server-forced-password-change/spec.md`, ограниченные тем, что реализовано (без `PATCH`/`POST /v1/users` — 26.2/26.3): вход с временным паролем выдаёт токены, прочие запросы отклоняются 403 `must_change_password`, разрешённое исключение (`change-password`) продолжает работать, успешная смена пароля снимает флаг и увеличивает `token_version`, неверный текущий пароль отклоняется и не снимает флаг, `change-password` работает и без временного статуса — `test/http/must_change_password_middleware_test.dart`, `test/http/routes/change_password_route_test.dart`
- [x] 26.7 Интеграционный сценарий (вместо расширения 10.1 — этот раздел ещё не в Этапе 1) покрыт `test/http/server_test.dart`/`test/bin/server_integration_test.dart` и вручную через реально запущенный процесс (см. 8.13): bootstrap-администратор → вход временным паролем → 403 `must_change_password` на `GET /v1/groups` → `POST /v1/auth/change-password` → тот же запрос успешен. Сценарий про `PATCH`/email-подтверждение — не в Этапе 1 (26.2/24)

## 27. structured_log_admin_client — редактирование пользователя и смена пароля (admin-client-resource-management, admin-client-auth) _(Этап 1 — частично, см. design.md «Delivery Phases»)_

- [x] 27.1 _(Этап 3 — без поля `email`)_ Экран редактирования пользователя (13.1, admin only): поля `display_name`/новый пароль (опционально), `PATCH /v1/users/:id`; предупреждение перед сохранением нового пароля о немедленном завершении сессий цели и обязательной смене пароля при следующем входе; список/экран пользователя обновляются немедленно после успеха. Поле `email` не показывается — раздел 24 не в объёме этапа (см. design.md «Delivery Phases»)
- [x] 27.2 Клиент перехватывает `403 must_change_password` на любом management/query-запросе (11.3, тот же уровень, что перехват 401) → принудительный экран смены пароля, блокирующий переход к остальным экранам, кроме выхода — перехват в `AuthInterceptor`, состояние в `SessionController.mustChangePassword` как **третье** состояние рядом с «вошёл»/«вышел»: сессия при этом полностью исправна, токены на месте, закрыто только всё остальное. `AuthGate` показывает экран вместо `HomeShell`. **Попутно исправлено:** 401 с кодом `invalid_grant` больше не считается протухшим токеном — так отвечает `change-password` на неверный текущий пароль, и обновление с повтором засчитывало бы серверу **вторую** неудачную попытку на пользовательский лимит, запирая учётку после нескольких опечаток (протухший токен сервер зовёт `unauthorized`)
- [x] 27.3 Принудительный экран смены пароля: поля текущего/нового пароля → `POST /v1/auth/change-password`; успех — переход на основной экран; неверный текущий пароль — ошибка, экран не покидается — `ForcePasswordChangePage` по `ForcePasswordChange.dc.html`, включая состояние «Пароль изменён». Сессию смена **не** рвёт, хотя сервер и увеличивает `token_version`: refresh-токен при этом не отзывается, поэтому следующий запрос обновляется штатным путём перехватчика. Имя пользователя для строки «Вошли как …» читается из claim `preferred_username` access-токена — `GET /v1/users/me` в Этапе 1 нет; подпись при этом **не** проверяется и проверяться не должна, claim нужен только чтобы подписать экран
- [x] 27.4 Действие «сменить пароль» в настройках (12.x, доступно всегда, не только при принудительной смене) → тот же `POST /v1/auth/change-password`; успех — подтверждение без выхода из сессии — диалог «Настройки аккаунта» из блока аккаунта в нав-панели, та же `ChangePasswordForm`, что и на принудительном экране. Профиль и «удалить аккаунт» с артборда `AccountSettings.dc.html` не сделаны: `GET /v1/users/me` и `DELETE /v1/users/me` в Этапе 1 отсутствуют
- [x] 27.5 Виджет/юнит-тесты — только на 27.2–27.4 (Delivery Phases): гейт поднимается из любого запроса и не роняет сессию, `invalid_grant` не вызывает обновление токена и повтор, три состояния `SessionController`, несовпадение новых паролей ловится на клиенте (лишний запрос стоил бы попытки у лимитера), неверный текущий пароль оставляет форму на месте, чтение `preferred_username`. Исходная формулировка ниже — на неё смотреть при возврате к 27.1. Сценарии: действие редактирования скрыто для не-admin, предупреждение перед новым паролем, обновление списка после успеха, `403 must_change_password` переводит на принудительный экран и блокирует остальную навигацию, успешная смена пароля открывает основной экран, неверный текущий пароль не снимает блокировку, смена пароля из настроек работает вне принудительного сценария

## 28. structured_log_server — ограничение частоты auth-эндпоинтов (log-server-rate-limit) _(Этап 1)_

- [x] 28.1 `ServerConfig`-поля ограничителя (`rateLimitEnabled`, ёмкость/скорость пополнения вёдер IP и субъекта, предел числа ключей, `trustedProxyHops`) — уже отмечено в 8.1
- [x] 28.2 Token bucket (`lib/src/http/rate_limit/token_bucket.dart`): ёмкость + непрерывное пополнение, ленивый пересчёт по `now - lastRefill` (без таймера на ключ), метод «попытаться списать токен» и метод «восстановить до полного»; возвращает время до появления следующего токена для `Retry-After`
- [x] 28.3 Хранилище вёдер (`lib/src/http/rate_limit/bucket_store.dart`): карта ключ → ведро с жёстким верхним пределом размера и LRU-вытеснением, периодический проход, выбрасывающий полные вёдра; состояние только в памяти, ничего в БД (decision 43 `design.md`)
- [x] 28.4 Определение клиентского IP (`lib/src/http/rate_limit/client_ip.dart`): адрес сокета по умолчанию, `X-Forwarded-For` — только при `trustedProxyHops > 0`, отсчёт элемента с конца списка
- [x] 28.5 Middleware ограничителя (`lib/src/http/rate_limit_middleware.dart`): применяется к закрытому списку путей из `specs/log-server-rate-limit/spec.md`. Проверка по IP — самая ранняя, до разбора тела запроса (отклонённый по IP запрос не стоит серверу даже парсинга); ключ субъекта берётся из уже разобранного тела (`username`/`email`) или из `sub` для аутентифицированных `POST /v1/auth/change-password`/`DELETE /v1/users/me`, поэтому его ведро проверяется непосредственно перед проверкой кредов, а списывается/восстанавливается по её исходу — хендлер сообщает middleware исход попытки (общий результат-тип, не throw)
- [x] 28.6 Ответ `429`: заголовок `Retry-After` (целые секунды по тому ведру, которое освободится позже) и общий JSON-конверт `{"error": "too_many_requests", ...}` — в том числе для `POST`/`DELETE /v1/auth/token`, единственное отступление от RFC-конверта (decision 43 `design.md`); запрошенное действие не выполняется (пароль не проверяется, письмо не отправляется, токены не выпускаются)
- [x] 28.7 Нормализация ключа субъекта (регистр `username`/`email`) — то же правило, что уже используется при поиске пользователя, чтобы ограничитель нельзя было обойти сменой регистра. **По факту нормализации нет, и это и есть «то же правило»:** поиск пользователя регистрозависим (`CREATE UNIQUE INDEX idx_users_username ON users (username)` без `COLLATE NOCASE`, `t.username.equals(...)`), то есть `Alice` и `alice` — разные учётные записи. Приведение к нижнему регистру сделало бы ограничитель строже поиска и связало бы счётчики двух разных аккаунтов; обходить же сменой регистра нечего — это попытка против другого аккаунта, что покрывается ведром IP. Задача сформулирована в предположении регистронезависимого поиска, которое для этой схемы не выполняется
- [x] 28.8 (`test/http/rate_limit_middleware_test.dart`, `test/http/rate_limit/`) Юнит-тесты на сценарии из `specs/log-server-rate-limit/spec.md`: приём/чтение логов и management не ограничиваются, доступ восстанавливается сам по времени, перебор не запирает аккаунт жертвы, успешный вход восстанавливает ведро субъекта, перебор одного `username` упирается в ведро субъекта, распыление по разным `username` — в ведро IP, несуществующий email ограничивается неотличимо от существующего, предел числа ключей соблюдается, `429` несёт `Retry-After` и общий конверт на token-эндпоинте, отклонённый запрос не отправляет письмо и не проверяет пароль, подделанный `X-Forwarded-For` игнорируется по умолчанию, при `trustedProxyHops = 1` учитывается адрес клиента, отключённый ограничитель не возвращает `429`
- [x] 28.9 Тест на монотонность времени: ведро пополняется по `Clock`/инжектируемому источнику времени, а не по `DateTime.now()` напрямую — иначе сценарии восстановления не проверить без реальных задержек

## 29. structured_log_server — аудит аутентификации (log-server-audit) _(Этап 2)_

- [x] 29.1 Новые значения `action` в закрытом наборе (раздел 19): `auth.login_succeeded`, `auth.login_failed`, `auth.logged_out`, `auth.throttled`
- [x] 29.2 Запись `auth.login_succeeded`/`auth.login_failed` в обработчике `grant_type=password` (3.5): `metadata` — `client_ip` (тот же расчёт, что 28.4), `user_agent`, для отказа — `reason` (`invalid_password`/`unknown_user`/`email_not_verified`/`blocked`/`deleted`); для неизвестного `username` — `actor_user_id: null` и `{"unknown_user": true}` без сохранения отправленной строки (decision 44 `design.md`)
- [x] 29.3 `grant_type=refresh_token` аудит-записей не создаёт — явная проверка в тестах, не только отсутствие кода
- [x] 29.4 Запись `auth.logged_out` в `DELETE /v1/auth/token` (3.7) — только для действительного токена; для неизвестного/уже отозванного (ответ не отличается, анти-enumeration) запись не создаётся
- [x] 29.5 Запись `auth.throttled` из middleware 28.5 — один раз на эпизод, в момент перехода ключа в исчерпанное состояние (флаг на самом ведре), `metadata` — `key_kind` (`ip`/`subject`), путь, `client_ip`; вне транзакции (у отказа нет мутации — decision 44 `design.md`)
- [x] 29.6 Юнит-тесты на сценарии из `specs/log-server-audit/spec.md` (аутентификация): успешный вход фиксируется с `client_ip`/`user_agent`, неудачный — с причиной, refresh не фиксируется, явный выход фиксируется, неизвестный `username` не попадает в аудит ни в каком виде, `auth.throttled` создаётся ровно один раз на эпизод
- [x] 29.7 Интеграционный тест (расширение `10.1` или отдельный сценарий): серия неудачных входов → `429` с `Retry-After` → `GET /v1/audit-log?action=auth.login_failed` показывает попытки, `GET /v1/audit-log?action=auth.throttled` — ровно одну запись на эпизод → после истечения окна вход с верным паролем успешен и даёт `auth.login_succeeded`

## 30. structured_log_admin_client — ограничение частоты и аудит аутентификации _(Этап 1 — частично, см. design.md «Delivery Phases»)_

- [x] 30.1 Перехват `429` в том же interceptor'е `dio`, что и `401` (11.3): чтение `Retry-After`, типизированная ошибка `RateLimited(retryAfter)` наверх через `Either` (decision 45 `design.md`); автоматического повтора нет — перехват `429` живёт в `failure_mapper.dart` (общий слой) и в `AuthRepositoryImpl` для RFC-конверта токен-эндпоинта; `Retry-After` читается, автоповтора нет
- [x] 30.2 Экран логина (12.1): сообщение об ограничении частоты с обратным отсчётом и блокировкой кнопки до его конца — отдельно от сообщения о неверных кредах — баннер ожидания с обратным отсчётом `mm:ss` и заблокированной кнопкой; сообщение отличается от «неверные креды». Сделано вместе с 12.1: требование про `429` входит в спеку самого экрана логина, и без него экран ей не соответствовал бы
- [ ] 30.3 Экраны восстановления пароля (18.x) и повторной отправки письма (25.1): то же сообщение об ожидании, одинаковое независимо от существования адреса
- [x] 30.4 Экран аудита (раздел 20): отображение `auth.*`-записей теми же фильтрами и списком, что административных — `client_ip`/`user_agent`/`reason` из `metadata` в читаемом виде; запись с `actor_user_id: null` показывается как попытка под несуществующей учётной записью, без запроса имени пользователя — сделано вместе с разделом 20. Запись без инициатора называет, какого именно «никого»: `unknown_user: true` в `metadata` читается как «учётной записи не существует», всё остальное (throttled, очистка) — как «сервер»; запроса за именем не уходит, потому что учётной записи нет, и это зафиксировано счётчиком запросов в `test/integration/audit_integration_test.dart`, а не только отсутствием кода
- [ ] 30.5 Виджет/юнит-тесты на сценарии из `specs/admin-client-auth/spec.md` (ограничение частоты) и `specs/admin-client-audit-log/spec.md` (аутентификационные события): `429` на логине показывает ожидание, а не ошибку кредов; сообщение при восстановлении пароля не зависит от существования адреса; повтор не происходит сам; фильтр `auth.login_failed` показывает причину и адрес; запись с `actor_user_id: null` отображается без инициатора. **Частично:** всё, что относится к Этапу 1, сделано — `429` на логине показывает обратный отсчёт и не выдаёт себя за ошибку кредов (`test/features/auth/login_flow_test.dart`, `auth_repository_test.dart`, `failure_mapper_test.dart`), повтор не происходит сам и сессия от `429` не заканчивается (`test/shared/api/auth_interceptor_test.dart` — там же зафиксировано, что `429` от токен-эндпоинта не превращается в обновление токена). Фильтры и отображение `auth.*` закрыты Этапом 2 вместе с 30.4 (`audit_screen_test.dart`, `audit_integration_test.dart`). Осталось одно: сообщение при восстановлении пароля — раздел 18, экранов ещё не существует

## 31. structured_log_server — срок хранения аудита (log-server-audit) _(Этап 2)_

- [x] 31.1 Поля `ServerConfig` (`auditRetentionDays`, `authEventRetentionDays`, размер порции удаления) — уже отмечено в 8.1; валидация: если срок задан, он SHALL быть положительным
- [x] 31.2 Классификация действий (`lib/src/audit/action_classes.dart`): явный список аутентификационных значений `action` и вывод административного класса как «все остальные» из того же закрытого набора — без колонки в БД и без `LIKE 'auth.%'` (decision 46 `design.md`); `audit.purged` относится к административному классу
- [x] 31.3 Тест-страж на полноту классификации: перечисляет все значения закрытого набора `action` и требует, чтобы каждое было отнесено ровно к одному классу — падает при добавлении нового действия, не внесённого ни в один список
- [x] 31.4 Два шага очистки аудита внутри уже существующего periodic purge job (7.3, новый таймер не вводится): удаление `auth.*`-записей старше `authEventRetentionDays` (`action IN (...)`, использует индекс по `action` из 2.2) и остальных старше `auditRetentionDays` (`action NOT IN (...)`); при незаданном сроке соответствующий шаг не выполняется вовсе
- [x] 31.5 Удаление порциями (`DELETE ... LIMIT N` в цикле до исчерпания подходящих строк), чтобы единственная длинная транзакция записи не блокировала приём логов в режиме WAL (decision 46 `design.md`); индекс по `audit_log_entries.created_at` — уже отмечено в 2.2
- [x] 31.6 Запись `audit.purged` (раздел 19) по итогам прохода, удалившего хотя бы одну строку: `actor_user_id: null`, `metadata` — `{"scope": "auth"|"admin", "deleted_count": N, "older_than": "<ISO 8601>"}`; проход, не удаливший ничего, записи не создаёт; одна запись на класс за проход, не на порцию
- [x] 31.7 Действующие сроки в ответе `GET /v1/audit-log` (`audit_retention_days`/`auth_event_retention_days` рядом с `items`/`next_cursor`, `null` = без ограничения) — расширение уже существующего ответа (5.x/раздел 19), не новый эндпоинт; отдельного API для изменения сроков нет и не добавляется
- [x] 31.8 Юнит-тесты на сценарии из `specs/log-server-audit/spec.md` (срок хранения): по умолчанию не удаляется ничего, `auth.*` удаляются по своему сроку при сохранении административных записей того же возраста, `audit.purged` создаётся только при непустом удалении и переживает удалённые им записи, ответ `GET /v1/audit-log` несёт действующие сроки, удаление идёт порциями (проверка на объёме, превышающем размер порции)
- [x] 31.9 Тест, фиксирующий отсутствие пути ручного удаления: ни один зарегистрированный маршрут не удаляет записи аудита (проверка по таблице маршрутов, а не по отсутствию кода)

## 32. structured_log_admin_client — срок хранения аудита _(Этап 2)_

- [x] 32.1 Отображение действующих сроков хранения из ответа `GET /v1/audit-log` на экране аудита (раздел 20): раздельно для аутентификационных событий и административных действий, с явным различением «без ограничения срока» и заданного значения — две плашки в шапке экрана, `describeRetention()` склоняет число и отдельно называет «без ограничения срока». Попутно закрыт пробел в развёртывании: `deploy/docker-compose.yml` не пробрасывал `STRUCTURED_LOG_AUDIT_RETENTION_DAYS`/`..._AUTH_EVENT_RETENTION_DAYS`, так что задокументированный деплой не мог задать сроки, которые этот экран показывает
- [x] 32.2 Пустой результат за пределами срока хранения объясняется сроком, а не обобщённым «ничего не найдено» (переиспользует пустое состояние из раздела 23, не заводит собственное) — `AuditState.isEmptyByRetention` включается, только когда запрошенный диапазон начинается раньше **меньшего** из двух порогов: истекает первым тот, что короче, он и мог унести искомое. Всё остальное объясняется фильтрами — сказать «эти записи удалены» про то, чего никогда не было, ровно такая же ошибка, как обратная
- [x] 32.3 Виджет/юнит-тесты на сценарии из `specs/admin-client-audit-log/spec.md` (срок хранения): оба срока показаны раздельно, пустой результат за границей срока объяснён, «без ограничения» отличается от заданного значения — в `audit_screen_test.dart`, включая случай «диапазон внутри срока — виноваты фильтры» и «срок не задан — retention не винится никогда»

## 33. Собственное логирование сервера и admin-клиента (decision 48) _(Этап 1)_

- [x] 33.1 Поля `ServerConfig` для логирования (`logLevel`/`logFile`/`logFormat`/ротация) — уже отмечено в 8.1; резолвятся тем же механизмом (раздел 8) и попадают в `--print-config`
- [x] 33.2 Инициализация логирования сервера (`lib/src/logging/setup.dart`): `StructlogConfiguration` с консольным выводом по умолчанию и `AsyncRotatingFileOutput` при заданном `logFile` (асинхронный — синхронная запись блокировала бы единственный isolate, decision 4/48); уровень и формат из конфигурации; вызывается в `bin/server.dart` до старта HTTP-сервера, чтобы ошибки конфигурации уже логировались штатно
- [x] 33.3 Request-логирование как shelf-middleware (`lib/src/http/logging_middleware.dart`): один `request.completed` на запрос с `method`/`path`/`status`/`duration_ms`, `request_id` через `LogCorrelation` и `BoundLogger.bind()`; уровень — `info` для 2xx/3xx, `warning` для 4xx, `error` для 5xx; тело запроса и заголовок `Authorization` не логируются никогда
- [ ] 33.4 Логирование фоновых процессов: старт/остановка сервера с эффективной конфигурацией (секреты — маской), каждый проход purge job (сколько удалено по retention логов и по обоим срокам аудита), срабатывание ограничителя частоты, ошибки отправки писем. **Частично:** старт/остановка — сделано (`server.starting` с контекстом из `maskedConfigContext`, `server.started`, `server.stopping`, плюс `bootstrap.warning`); срабатывание ограничителя — сделано (`rate_limit.throttled`, раздел 28). purge job — сделано (`retention.purged` с числом удалённых записей, освобождённых байт и затронутых проектов, `retention.purge_completed` на пустом проходе, `retention.purge_failed`). Счётчики по обоим срокам аудита добавлены Этапом 2 — `retention.purged` несёт `deleted_admin_audit`/`deleted_auth_audit` рядом с `deleted_entries`. Осталось одно: ошибки отправки писем — раздел 17 (`EmailSender` ещё не существует)
- [x] 33.5 Инициализация логирования клиента (`lib/shared/logging/setup.dart`): консольный вывод в debug-сборке, `warning` и выше в release; `BoundLogger` прокидывается через DI (`cherrypick`, decision 35), а не берётся глобально в виджетах — `lib/shared/logging/setup.dart`: консоль, `debug` в debug-сборке и `warning`+ в release через `minLevel` синка; `BoundLogger` раздаётся через DI (11.5), глобально в виджетах не берётся
- [x] 33.6 Тест-страж (`test/logging/no_print_test.dart`): в `structured_log_server` нет вызовов `print(`/`debugPrint(`/`dart:developer` (проверка по `lib`/`bin`/`test`, кроме `example/` и сгенерированного кода); нарушение роняет тест. Сам страж проверен на самопроверке: отдельный кейс убеждается, что его регулярки ловят образцы нарушений и не срабатывают на `stdout.writeln`/`buffer.write`. Часть про `structured_log_admin_client` — вместе с 33.5
- [x] 33.7 Тест: собственная диагностика сервера не попадает в `log_entries` (после серии запросов и прохода purge job таблица содержит только принятые по `POST /v1/logs` записи) и не подменяет аудит
- [x] 33.8 Тест на отсутствие утечек: логи, порождённые обработкой `POST /v1/auth/token`, `POST /v1/auth/change-password` и `POST /v1/logs`, не содержат ни пароля, ни access/refresh-токена, ни секретного ключа проекта

## 34. Автосоздание первого администратора при старте (log-server-auth, decision 49) _(Этап 1)_

- [x] 34.1 Поля `ServerConfig` (`bootstrapAdminEnabled`/`bootstrapAdminUsername`/`bootstrapAdminPassword` + `*_FILE`) — уже отмечено в 8.1; пароль резолвится тем же кодом секретов, что 8.5, и маскируется в `--print-config` (8.9)
- [x] 34.2 Генерация пароля (`lib/src/auth/bootstrap_admin.dart`): при незаданном `bootstrapAdminPassword` — `generateRandomToken()` (`lib/src/auth/hashing.dart`, `Random.secure()`, та же схема, что для секретных ключей и токенов, decision 11)
- [x] 34.3 Проверка «таблица `users` пуста» и вставка администратора в **одной транзакции**, до открытия порта (`bin/server.dart` → `_runServe`, до `shelf_io.serve`): роль `admin` на глобальной области (`RoleAssignment`), `is_primary_admin = true`, `must_change_password = true`, `email` не собирается
- [x] 34.4 Однократный вывод сгенерированного пароля уровнем `warning` с пометкой «временный, смените при первом входе» — через инъекцию `logWarning` (собственное логирование сервера, раздел 33, ещё не построено — `bootstrapAdmin` не завязан на конкретный механизм лога заранее); пароль, заданный через окружение, не логируется никогда
- [x] 34.5 Три случая пропуска, все — штатный исход, не ошибка: (а) база не пуста при заданных bootstrap-переменных — предупреждение, что параметры не применены, пароль существующего администратора не меняется; (б) база пуста, но автосоздание выключено — предупреждение с подсказкой запустить `create-admin`; (в) проигрыш гонки за вставку (`SqliteException` от уникального частичного индекса на `is_primary_admin`, раздел 2.2, перехватывается) — то же предупреждение, что в (а), старт продолжается. Если база не пуста и bootstrap-переменные не заданы — ничего не логируется
- [ ] 34.6 _(не в Этапе 1)_ Аудит-запись `user.created` (раздел 19) с `actor_user_id: null` и `metadata` `{"bootstrap": true, "password_source": "env"|"generated"}` — раздел 19 (аудит) не в Этапе 1
- [x] 34.7 Юнит-тесты на сценарии из `specs/log-server-auth/spec.md` (автосоздание), ограниченные тем, что реализовано (без аудита — 34.6, без реальной параллельной гонки — race-catch протестирован опосредованно через исключение, не через два настоящих процесса): пустая база даёт администратора с обоими флагами и без email, непустая база остаётся нетронутой, заданные параметры на непустой базе дают предупреждение и не меняют существующий пароль, параметры по умолчанию на непустой базе не логируют ничего, выключатель отключает создание (с предупреждением на пустой базе, без — на непустой), кастомный username учитывается — `test/auth/bootstrap_admin_test.dart` (9 тестов). Подтверждение «вход не требует подтверждения email» — тривиально верно в Этапе 1, где email-верификации нет вообще (раздел 24 не в Этапе 1)
- [x] 34.8 Тест: сгенерированный пароль появляется в предупреждении ровно один раз и только при фактическом создании; заданный через конфигурацию — не появляется вовсе — покрыто в `test/auth/bootstrap_admin_test.dart`; маскирование в `--print-config` — уже покрыто в `test/config/config_resolver_test.dart` (раздел 8.12), одним и тем же механизмом для любого секрета, не специфичным для bootstrap-пароля
- [x] 34.9 Интеграционный сценарий (без `GET /v1/audit-log` — аудит не в Этапе 1) покрыт `test/bin/server_integration_test.dart` и подтверждён вручную на реально запущенном процессе: пустая БД → старт сервера → сгенерированный пароль в предупреждении → вход этим паролем (`grant_type=password`) успешен → `GET /v1/groups` отклонён `403 must_change_password` → `POST /v1/auth/change-password` → тот же запрос успешен → создание группы/проекта/ключа → приём лога → запрос лога — весь путь пройден через настоящий HTTP

## 35. Развёртывание через docker-compose _(по прямому запросу пользователя 15.09.2026; вне исходного объёма change)_

- [x] 35.1 `backend/structured_log_server/Dockerfile` — многоэтапный: `dart:3.12` собирает AOT-бинарь (с `build_runner`, без которого пакет не компилируется), рантайм — `debian:bookworm-slim`, а не `scratch`: `drift` ходит в libsqlite3 через `dart:ffi`, и эта библиотека обязана быть в образе. Плюс `curl` — чтобы healthcheck бил в `/healthz`, а не просто доказывал, что бинарь стартует. Процесс работает под непривилегированным uid; контекст сборки — корень репозитория, чтобы сервер собирался против рабочей копии `structured_log`, а не опубликованной (тот же `pubspec_overrides.yaml`, что пишет CI)
- [x] 35.2 `frontend/structured_log_admin_client/Dockerfile` — nginx со статикой клиента; `build/web` собирается **на хосте** (`deploy/deploy.sh`) тем же FVM-SDK, что и весь репозиторий. Официального Docker-образа Flutter не существует, а сторонний в цепочке деплоя — непроверенная зависимость там, где она дороже всего
- [x] 35.3 `deploy/docker-compose.yml` + `nginx.conf`: `server` не публикуется наружу, `web` проксирует `/v1/` на него. **Один origin вынужденный, а не стилистический** — CORS в сервере нет нигде (ни middleware, ни спеки, ни design.md), поэтому клиент с другого хоста не смог бы обратиться к API вообще; бандл собирается с пустым base URL, и один образ работает на любом домене. SSE-маршрут не буферизуется и не режется по таймауту
- [x] 35.4 `deploy/deploy.sh`: проверки окружения, генерация `secrets/jwt_signing_secret` при первом запуске (файл, а не переменная — не виден ни в `docker inspect`, ни в списке процессов), сборка web, `compose build`/`up`, подсказка, где взять сгенерированный пароль администратора. `deploy/.env` и `deploy/secrets/` — в `.gitignore`
- [x] 35.5 `deploy/README.md`/`README.ru.md` — билингвально, по конвенции репозитория. **Проверено на живом развёртывании:** `./deploy.sh` с нуля собирает оба образа и поднимает стенд, сервер проходит healthcheck, через прокси отвечают `/healthz`, SPA-роуты (включая deep link) и `/v1/*` с ответом 401 без токена, вход админом с автосозданным паролем возвращает пару токенов, UI отдаётся из контейнера и рисует ответ сервера. Не проверено сквозным кликом: ввод в поле пароля синтетическими событиями в Flutter-канвас не доходит — ограничение инструмента, а не приложения; путь входа подтверждён HTTP-запросом через тот же прокси
- [x] 35.6 **Имя параметра секрета приведено к решению, а не документация к коду.** decision 47 `design.md` трижды называет его `STRUCTURED_LOG_JWT_SECRET`, вся `docs/operations/configuration.md` — тоже; реализация разошлась, назвав параметр `jwt-signing-secret`, и это разошлось незамеченным (задача 8.1 зафиксировала имя из кода). Обоснования у длинного имени нигде нет, поэтому переименован код: `jwt-secret` ↔ `STRUCTURED_LOG_JWT_SECRET`(`_FILE`). Затронуты `server_config.dart`, тесты, README сервера и `deploy/`. Ломающее изменение для развёрнутых установок — их нет: сервер не выпущен, а `deploy/` появился накануне. Из документации, где были описаны ещё не реализованные параметры (`--registration-enabled`, SMTP, retention аудита), ничего не удалялось: `docs/` описывает спроектированную систему целиком, это оговорено в `AGENTS.md`
- [x] 35.7 _(дефект сервера, найден при проверке развёртывания)_ **`POST`/`DELETE /v1/auth/token` отвечали 500 на некорректное тело формы.** `Uri.splitQueryString` бросает `ArgumentError` при illegal percent encoding, а хендлеры его не ловили — в ответ уходил `500 Internal Server Error`, а в лог стектрейс; это **pre-auth** эндпоинт, туда приходит ровно тот ввод, который никто не контролирует. Исправлено: `tryParseFormBody()` в `lib/src/http/request_helpers.dart` возвращает `null` вместо броска (а не бросает `ApiError`, потому что токен-эндпоинты отвечают в конверте RFC 6749, а не в общем — decision 10), хендлеры отдают `400 invalid_request`. **Тот же дефект нашёлся третьим местом:** `POST /v1/logs` делал голый `jsonDecode`, и кривой JSON давал 500 — теперь через `readJsonArrayBody()`, парный уже существовавшему `readJsonBody()`. Тесты: тело `%zz` на обоих токен-эндпоинтах и оборванный JSON на приёме логов
