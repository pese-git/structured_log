## 1. Сервер: общий контракт

- [ ] 1.1 Функция разбора `limit`/`cursor` в `lib/src/http/` (рядом с `log_query_params.dart`) → `PageRequest`; по умолчанию 50, предел 200 (приведение), нецелое/`< 1`/недопустимый курсор → `ApiError.badRequest`. Заменяет три копии `int.parse` в `logs_route.dart`, `audit_log_route.dart`, `users_route.dart`.
- [ ] 1.2 Общий помощник страницы: запрос `limit+1`, обрезка, `next_cursor` от последнего показанного элемента, `null` на последней странице. Переводит на него `LogStore` (сейчас выдаёт курсор на непустой странице), аудит и пользователей.
- [ ] 1.3 Тесты: предел, значение по умолчанию, `limit=0`/`-5`/`abc`, недопустимый курсор, последняя страница при ровно `limit` элементах, порядок ролей и разбора (`owner` с плохим `limit` получает `403`, не `400`).
- [ ] 1.4 Тест «все пагинированные списки ведут себя одинаково» — один набор параметров по очереди на каждый из пяти эндпоинтов.
- [ ] 1.5 Устранить `GET /v1/users?limit=0` → пустая страница и обращение к `page.last` (закрывается 1.1/1.2; тест регрессии).

## 2. Сервер: видимость в SQL

- [ ] 2.1 В `lib/src/rbac/` функция, строящая условие видимости групп и проектов из набора ролей вызывающего (`admin` — без условия) тем же источником данных, что читает `canRead`.
- [ ] 2.2 Тест на совпадение: для каждой комбинации роли (`admin`/`owner`/`user`) и области (`global`/`group`/`project`) множество из SQL равно множеству, отобранному `canRead` в памяти.
- [ ] 2.3 Проверить предел числа переменных SQLite при длинном списке ролей; выше порога — подзапрос к `role_assignments` вместо литералов (решение о способе фиксируется по результату проверки, не предполагается).

## 3. Сервер: группы и проекты постранично

- [ ] 3.1 `GET /v1/groups`: `limit`/`cursor`/`next_cursor`, `name` комбинируется с ними, порядок `id DESC`, условие видимости из 2.1.
- [ ] 3.2 `GET /v1/projects`: то же плюс `group_id`.
- [ ] 3.3 Тесты: пользователь с одной группой среди 500 получает её на первой странице без курсора; обход `admin` страницами равен полному набору; поиск по имени + курсор; группа, созданная между страницами, не ломает курсор; **сверка обхода `GET /v1/projects` с `GET /v1/projects/:id` по всем проектам базы**.
- [ ] 3.4 Добавить `GET /v1/groups` и `GET /v1/projects` с новыми параметрами в таблицу `test/http/route_auth_matrix_test.dart`, если параметры меняют разбор; порядок проверок «роль → параметры → хранилище» закрепить тестом.

## 4. Сервер: индекс (по замеру)

- [ ] 4.1 Замерить `GET /v1/logs` (`ORDER BY id DESC`, фильтр по `project_id`) на порядке миллионов записей на проект с индексом `(project_id, id)` и без него; замерить также стоимость приёма с индексом.
- [ ] 4.2 Добавить индекс и миграцию схемы drift только если 4.1 показал выигрыш, не отменяемый замедлением приёма; иначе записать решение и цифры в этот файл и не менять схему.

## 5. Клиент: слой данных

- [ ] 5.1 Retrofit-интерфейсы и DTO списков групп/проектов получают `limit`/`cursor` и `next_cursor` в ответе; репозитории возвращают страницу (`items` + курсор), а не список.
- [ ] 5.2 Обновить `lib/testing/mock_server.dart`: страницы и `next_cursor` для групп и проектов — тем же форматом провода, что сервер (мок отвечает картами, а не DTO клиента).
- [ ] 5.3 `RoleAssignmentsRepositoryImpl.searchUsers`: перестать выбрасывать `next_cursor`.
- [ ] 5.4 Аудит: передавать `limit` явно, как `QueryLogs.pageSize` и `UsersCubit._pageSize`.

## 6. Клиент: экраны

- [ ] 6.1 Общий помощник состояния списка «страница + курсор + сброс при смене фильтра»; перевести на него `GroupsCubit`, `UsersCubit`, `AuditCubit`.
- [ ] 6.2 Экран групп: «Показать ещё»/подгрузка; действие пропадает при `next_cursor: null`.
- [ ] 6.3 Дашборд: `limit=3` проектов, без загрузки всех групп и проектов; порядок карточек теперь детерминирован.
- [ ] 6.4 `ScopeSelector` и пикеры (`AdminSearchPicker`): поиск через `?name=`/`?username=`, подгрузка либо явное «показана часть»; не молчаливое усечение.
- [ ] 6.5 Лента логов: не выполнять запрос после `next_cursor: null`; предел удерживаемых записей с отбрасыванием с дальнего конца и восстановлением признака «есть ещё» (`design.md`, решение 10).
- [ ] 6.6 Тесты каждого потребителя: создать больше элементов, чем помещается на страницу, и проверить, что последний достижим; тест на положение прокрутки ленты при отбрасывании.
- [ ] 6.7 Браузерный `integration_test/user_flow_test.dart`: сценарий со списком длиннее страницы (группы или пользователи), прогнать `flutter drive` тем же способом, что CI.

## 7. Сквозные тесты и документация

- [ ] 7.1 `packages/e2e`: обход списков страницами через настоящий сервер и репозитории клиента.
- [ ] 7.2 `docs/api/http-api.md`/`.ru.md`: общий раздел про пагинацию (`limit`, предел, `next_cursor`, ошибки), пометка о ломающем изменении `GET /v1/groups`/`/v1/projects`.
- [ ] 7.3 `AGENTS.md`: абзац о контракте пагинации и о том, что небольшие списки намеренно не пагинируются, с условиями пересмотра из `design.md`.
- [ ] 7.4 `dart analyze`, `dart format --set-exit-if-changed`, `dart test`/`flutter test` по затронутым пакетам; CI зелёный на всех джобах.
