# FVM

# Dart
.dart_tool/
.packages
build/
*.iml

# Melos-managed local dependency overrides (regenerated by `melos bootstrap`)
pubspec_overrides.yaml

# Packages resolve their own dependencies for consumers; lockfiles aren't
# needed for reproducible library builds and only churn between path/hosted
# resolution around publish.
pubspec.lock

# Coverage
coverage/

# Logs
*.log
logs/

# IDE
.idea/
.vscode/
*.swp
*.swo

# macOS
.DS_Store

# FVM Version Cache
.fvm/

# AI agent tooling
.claude/
.codegraph/
.codex/
.opencode/
# Deployment state, never committed: .env is per-host, and the signing secret
# lets whoever holds it mint access tokens for any account.
deploy/.env
deploy/secrets/
