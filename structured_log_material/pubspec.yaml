name: structured_log_material
description: >-
  A Material 3 in-app log viewer for structured_log — live list, expanded
  entry detail, and empty states, built on structured_log_flutter.
version: 0.1.0-dev.1
repository: https://github.com/pese-git/structured_log.git
publish_to: none

environment:
  sdk: ^3.0.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  structured_log: ^0.2.0-dev.0+1
  structured_log_flutter: ^0.1.0-dev.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^3.0.0
