name: structured_log_workspace
description: Melos workspace root for the structured_log monorepo — not a published package.
publish_to: none

environment:
  sdk: ^3.0.0

dev_dependencies:
  melos: ^6.0.0
