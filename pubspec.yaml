name: structured_log_workspace
description: Melos workspace root for the structured_log monorepo — not a published package.
publish_to: none

environment:
  sdk: ^3.0.0

dev_dependencies:
  melos: ^6.0.0
  # For `test/coverage_floor_test.dart`, which covers the only code this
  # root package holds: `tool/coverage_floor.dart`, the coverage gate CI
  # runs for every package.
  test: ^1.25.0
